# 04 AII共识机制详细设计

> 版本：v0.1（初稿）
> 适用对象：共识算法实现工程师、安全审计方、节点运营商
> 关联：本文档为《02 黄皮书》§4 的详细展开。

---

## 1. 设计原则

1. **分层共识**：主链与子链解耦，各自选择最优算法。
2. **可插拔**：子链共识通过统一接口（见《03 架构设计文档》§5.1）注入。
3. **经济安全**：所有出块/验证职责都伴随抵押与罚没。
4. **可治理**：所有共识参数可通过链上提案修改。
5. **可演进**：保留协议级版本协商，便于后续算法切换。

---

## 2. 主链共识：PoW + V 节点签名（混合）

### 2.1 出块流程形式化

设当前主链高度 `n`，候选区块 `B_{n+1}`：

```
1. 矿工 M 收集 mempool 中可打包交易，构造 B_{n+1} 候选。
2. M 计算 Ethash(header_without_mixHash, nonce) 寻找满足
   Ethash(...) ≤ 2²⁵⁶ / difficulty(n+1) 的 nonce*。
3. M 将 (header, txs, ommers, flushList) 发给当前轮值 V 节点 V*。
4. V* 验证：
   a) Ethash 合法
   b) header 字段与交易一致
   c) flushList 中每个 Flush 的 SCS 共识证明有效
5. 若全部通过，V* 对 keccak(RLP(header[1..18])) 用 BLS 私钥签名得到 vNodeSig。
6. M 将 vNodeSig 写入 header 第 19 字段，广播完整 B_{n+1}。
7. 全网节点验证 PoW + vNodeSig，达成共识。
```

### 2.2 V 节点轮值

V 节点集合 `V_active`（默认 21 名，最大 101 名）按 stake 加权轮值：

```
chooseVNodeForBlock(blockNum n) -> V_i :
    seed = H(parentHash || epochNum)
    pick V_i with probability ∝ stake_i / sum(stake)
    using deterministic PRF(seed, n)
```

轮值 V 节点错过签名超时窗口（默认 2 秒）后，自动切换至下一顺位 V 节点（防止单点拖累出块）。

### 2.3 难度调整算法

继承以太坊 EIP-100：

```python
def calc_difficulty(parent, timestamp):
    diff = parent.difficulty
    adj = diff // 2048
    factor = max(1 - (timestamp - parent.timestamp) // 9, -99)
    diff = max(D_MIN, diff + adj * factor)
    return diff
```

**无难度炸弹**（移除 MOAC 与以太坊原有的"冰川"机制，避免对 PoW 阶段过渡的不必要约束）。

> **差异化决策**：本链不引入难度炸弹，转而通过治理投票决定何时启动 PoS 切换。理由：PoS 切换涉及生态多方利益，应由治理决策而非协议强制。

### 2.4 V 节点准入与退出

#### 加入

```
JoinV(addr A) :
    require balanceOf(A) ≥ S_min
    require A.signature ∋ valid VRF + BLS public keys
    require A.hardware_pass = true   // 可选 KYC
    
    transfer S_min from A to VNodeStaking contract (锁定)
    insert A into V_pending (need 1 epoch warmup)
    after warmup -> V_active
```

#### 退出

```
LeaveV(addr A) :
    require A ∈ V_active
    move A.stake from V_active to V_cooling
    unfreeze after T_unstake = 14 days
    if no slashing event during cooling -> return stake to A
```

#### 抵押要求

| 参数 | 默认值 |
|---|---|
| `S_min(V)`（V 节点最少抵押） | `《[占位] 50,000》` 枚原生代币 |
| `S_max(V)`（单 V 节点最大有效权重） | `《[占位] 200,000》` |
| `T_warmup`（预热期） | 1 epoch（≈1 天） |
| `T_unstake`（冷却期） | 14 天 |
| `Reward(V)`（V 节点平均年化） | 8–15% |

### 2.5 V 节点罚没（Slashing）

| 触发事件 | 检测方式 | 罚没比例 |
|---|---|---|
| 双签（同一高度两个不同区块） | 任何节点提交两份 vNodeSig 即可证明 | 100% |
| 缺席连续 10 次轮值 | 协议自动检测 | 永久除名 + 抵押全没收 |
| 缺席 Flush ≥ 3 次 | 协议自动检测 | 5% / 次 |
| 提交无效跨链证明 | 任何节点提交反证 | 10% |
| 与算力勾结作 51% 重组（事后证明） | 治理仲裁 | 100% |

罚没金分配：
- 50% 销毁
- 30% 进入举报者奖励池
- 20% 进入安全保险基金（Insurance Pool）

---

## 3. 子链共识引擎接口

详见《03 架构设计文档》§5.1。每种共识算法需实现该接口并注册。下面逐一展开默认提供的四种内置共识。

---

## 4. 子链共识算法

### 4.1 PoS（默认）

#### 4.1.1 选举

每个 epoch（默认 `《[占位] 6》` 小时），按 stake 加权随机选出当 epoch 出块者列表：

```
seed = H(prev_epoch_seed || prev_epoch_finalHash)
for each slot in epoch:
    proposer = weighted_random_pick(all_validators, stake_weights, slot, seed)
```

#### 4.1.2 出块与确认

```
slot_time = 2 秒（可治理）
每个 slot 由 proposer 提议区块
每个 attester（验证人）对区块签名
当区块收到 ≥ ⅔ stake 加权签名 -> attested
当被后续 N=64 个区块再次 attest -> finalized
```

#### 4.1.3 PoS 罚没

| 事件 | 罚没 |
|---|---|
| 双签 | 100% |
| 反向投票（vote 之前确认的区块）| 部分罚没（与作恶面成正比） |
| 长时间离线（>20% epoch slots） | 0.5% / epoch |

#### 4.1.4 PoS 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `S_min(PoS)` | `《[占位] 1,000》` 代币 | 单 SCS 入门门槛 |
| Slot 时间 | 2 秒 | – |
| Epoch 长度 | 10800 slots（6 小时） | – |
| 最终性窗口 | 2 epochs | – |
| 提议者数 | ≥ 64 | – |

### 4.2 PBFT（联盟/金融子链）

#### 4.2.1 算法

经典 PBFT 三阶段：`Pre-Prepare → Prepare → Commit`。

```
节点数 n，恶意节点上限 f = (n-1)/3
区块由轮值 leader 提议
每轮三阶段完成后区块即最终
view-change：leader 超时未提议 -> 触发更换 leader
```

#### 4.2.2 BLS 聚合签名优化

每阶段的签名集合用 BLS12-381 聚合，单签名验证开销 O(1)。

#### 4.2.3 PBFT 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `n`（节点数） | 7 / 21 / 51 | 可治理 |
| `view_timeout` | 6 秒 | leader 超时 |
| 区块时间 | 1–3 秒 | – |
| 最终性 | 单区块 | – |
| `S_min(PBFT)` | `《[占位] 5,000》` 代币 | – |

### 4.3 DPoS（高 TPS 游戏）

#### 4.3.1 见证人选举

```
全网代币持有者投票选出 21 名见证人（数字可治理）。
每 21 个 slot 一轮，每位见证人按确定顺序产 1 块。
slot 时间默认 0.5 秒，1 轮 10.5 秒。
```

#### 4.3.2 缺席处罚

连续缺席 ≥ 3 次自动从见证人池剔除，由备选见证人补位。

#### 4.3.3 DPoS 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| 见证人数 | 21 | 可治理 1–101 |
| Slot 时间 | 0.5 秒 | – |
| 投票冷却 | 24 小时 | 防止快速切换 |
| 最终性 | 单区块（弱）；2/3 见证人确认（强） | – |

### 4.4 Custom（自定义）

子链发起方可上传自己的共识引擎 WASM 二进制（或注册到主链合约的引擎 ID）。审核机制：

- 必须通过协议级共识引擎兼容性测试（CETF：Consensus Engine Test Framework）
- 必须通过AII DAO安全审计
- 必须有 ≥ S_min(Custom) = `《[占位] 100,000》` 代币抵押作为质量背书

---

## 5. V 节点 ↔ SCS 节点协调

### 5.1 子链调度

每条子链对应一个由 V 节点抽签产生的**协调者（Coordinator）**：

- 协调者每个 epoch 重新抽签
- 职责：
  - 监听该子链的状态与共识进度
  - 在 Flush 周期提交 Flush 交易到主链
  - 触发 SCS 节点出错时的应急处理（请求其他 V 节点见证、提交罚没证据）

### 5.2 Flush 流程

```
每 T_flush 个主链块（默认 30，≈ 5 分钟），协调者：

1. 拉取子链最新区块 B_s（要求已达到子链最终性）
2. 拉取 B_s 包含的所有共识证明 σ_proof
3. 构造 Flush 交易：
   Flush = {
       microChainId,
       fromHeight = lastFlush.toHeight + 1,
       toHeight   = B_s.height,
       stateRoot  = B_s.stateRoot_s,
       receiptsRoot = B_s.receiptsRoot_s,
       scsSet     = currentSCSValidators,
       proof      = σ_proof,
   }
4. 提交 Flush 交易到主链
5. 主链全局合约 MotherChainRegistry 验证 proof，写入子链注册表
6. 触发 SCS 节点奖励发放事件
```

### 5.3 异常处理

| 情况 | 处理 |
|---|---|
| 子链 stalled（>2×Flush周期无新块） | 协调者上报，触发子链状态调查；V 节点投票决定是否暂停子链 |
| 子链双链分叉 | 协调者选择具有更高经济权重的链；若难以决断，提交治理仲裁 |
| 协调者本身宕机 | 其他 V 节点检测到协调者无响应，超时后选举新协调者 |
| Flush 证明无效 | 提交者被罚没 |

---

## 6. SCS 节点准入

SCS 节点参与某条子链需要：

1. 抵押：≥ 该子链定义的 `S_min(C)`
2. 共识身份验证：BLS / VRF 公钥注册到 `MotherChainRegistry`
3. 子链发起方批准（实验级子链可选）

SCS 节点退出冷却期：默认 7 天。

---

## 7. 跨子链消息共识保障

跨链消息从源子链到目的子链的端到端流程涉及 4 个共识点：

1. **源子链共识** → 消息事件被源子链共识确认
2. **源子链 Flush** → 主链共识确认事件已经发生
3. **目的子链协调者** → 监听主链事件
4. **目的子链共识** → 消息被纳入目的子链区块

每一步都有相应的共识证明可链上验证。

---

## 8. PoW → PoS 长期演进路径

本链主网启动时为 **PoW + V 节点签名混合**。未来路径：

| 阶段 | 主链共识 | 触发条件 |
|---|---|---|
| Genesis | PoW + V 节点签名 | 主网上线 |
| Phase X | 混合算力权重逐步下降，V 节点权重上升 | 治理投票 |
| Phase Y | 完全 PoS（保留 V 节点架构） | 治理投票 + 安全审计 |

> **差异化决策**：不强行设置硬切换时间。理由：以太坊 The Merge 经验表明，社区准备度、矿工迁出节奏、生态稳定性都需要时间评估。

---

## 9. 共识安全证明思路（待形式化）

### 9.1 PoW + V 节点混合的攻击成本

设算力总值 H、stake 总值 S，则 51% 攻击成本：

```
Cost_attack ≥ 0.51 × H + 0.67 × S × P_token
```

相比纯 PoW，攻击者必须同时购买算力**并**积累 ≥ ⅔ V 节点抵押，攻击成本翻倍以上。

### 9.2 PBFT 子链的 ⅓ 容错

经典 PBFT 证明：当 f ≤ (n-1)/3 时，系统满足安全性（不会同时确认两个矛盾区块）与活性（在网络同步期内能持续出块）。

### 9.3 长程攻击防护

子链区块头携带 `anchorMotherBlock`，记录子链锚定的主链高度。任何"穿越历史"的子链分叉必须能产出一条更长且锚定更早主链的链，但由于主链 PoW 不可篡改，攻击不成立。

---

## 10. 共识可治理参数清单

下列参数都通过链上治理可调（详见《07 治理与AII DAO架构》§3）：

| 参数 | 默认 | 调整窗口 |
|---|---|---|
| V 节点最大数量 | 21 | 21–101 |
| `S_min(V)` | 50,000 | ±50% |
| `T_unstake` | 14 天 | 7–28 天 |
| `T_flush` | 30 块 | 10–100 块 |
| 子链 PoS slot 时间 | 2 秒 | 1–10 秒 |
| 主链出块时间 | 10 秒 | 5–15 秒 |
| 主链 gasLimit | 30M | 由协议自动 ±1/1024 |
| 双签罚没 | 100% | 不可调（协议级） |
| 区块奖励初始值 | 2 代币 | 治理可调，年内调整次数 ≤ 1 |

---

## 本批次占位字段汇总

| 占位符 | 出现位置 | 建议候选 |
|---|---|---|
| `《[占位] 50,000》` | §2.4 V 节点抵押 | 与黄皮书一致 |
| `《[占位] 200,000》` | §2.4 V 节点权重上限 | – |
| `《[占位] 6》`小时 | §4.1 PoS epoch | – |
| `《[占位] 1,000》` | §4.1 PoS 入门 | – |
| `《[占位] 5,000》` | §4.2 PBFT 入门 | – |
| `《[占位] 100,000》` | §4.4 Custom 共识 | – |

— 本共识机制设计完 —
