# 04 AII共识机制详细设计

> 版本：v0.1（初稿）
> 适用对象：共识算法实现工程师、安全审计方、节点运营商
> 关联：本文档为《02 黄皮书》§4 的详细展开。

---

## 1. 设计原则

1. **分层共识**：主链与子链解耦，各自选择最优算法。
2. **可插拔**：子链共识通过统一接口（见《03 架构设计文档》§5.1）注入。
3. **经济安全**：所有出块/验证职责都伴随抵押与罚没。
4. **可治理**：所有共识参数可通过链上提案修改。
5. **可演进**：保留协议级版本协商，便于后续算法切换。

---

## 2. 主链共识：BFT-PoS（单区块即时最终性）

> **设计决策**：AII 主链采用**纯 PoS** + **Tendermint 风格 BFT 共识**，**不引入 PoW**。
>
> 设计理由：
> - **能效**：无算力竞争，单台普通服务器即可参与共识，符合 2026 年后绿色公链趋势
> - **即时最终性**：BFT 单区块即最终，无重组、无概率最终
> - **公平准入**：硬件门槛低，门槛由抵押决定，任何资金可控的参与方均可加入
> - **跨链友好**：单区块最终性极大简化跨链证明
> - **抗审查**：相对纯 DPoS 验证者更多（21–101），相对 PoW 无矿池中心化

### 2.1 时间结构（Slot + Epoch）

主链时间按 **slot** 划分：

| 单位 | 默认值 | 说明 |
|---|---|---|
| **slot** | 6 秒 | 一个 slot 产出至多 1 个区块；可治理 |
| **epoch** | 32 slots = 192 秒（约 3 分钟） | 验证者集合每 epoch 重选；Flush 周期与 epoch 对齐 |

### 2.2 出块流程（每 slot）

```
slot s 开始:
1. 提议者选举：
   proposer_s = VRF(seed_epoch, validators_weighted_by_stake, slot_s)
   
2. proposer 收集 mempool 交易 + Flush + 跨链证明，构造区块 B_s

3. proposer 用 BLS 私钥对 RLP(header_s) 签名 → sig_propose
   广播 (B_s, sig_propose)

4. 其他活跃 V 节点收到 B_s，验证：
   - header 字段一致
   - parent 是当前最新最终块
   - 交易合法（签名、nonce、余额、gas）
   - Flush 证明有效
   若通过，发出 PRE-VOTE(B_s) 消息（BLS 签名）

5. 当任一节点收集到 ≥ ⅔ stake 加权的 PRE-VOTE：
   该节点广播 PRE-COMMIT(B_s)

6. 当任一节点收集到 ≥ ⅔ stake 加权的 PRE-COMMIT：
   B_s 被该节点视为 FINALIZED
   该节点更新本地链顶到 B_s
   PRE-COMMIT 集合写入 B_{s+1}.header.commitSig 作为 B_s 的 commit 证书

7. 若 slot 超时（proposer 未广播 / 没达成 ⅔ PRE-COMMIT）：
   该 slot 跳过，进入 slot s+1，新 proposer 接棒
   超时 slot 不产块（链高度不增）
```

**特点**：

- **单区块最终性**：B_s 在 slot s 末即被全网视为最终（**无需等 6 块概率确认**）
- **无叔块（uncle）**：BFT 共识不分叉，故无 uncle 概念
- **跳过容忍**：若 ⅓ 以上 V 节点离线，链停滞但不分叉

### 2.3 提议者选举

每 epoch 初，根据上一 epoch 终末状态 + VRF 种子，**预计算**本 epoch 全部 slot 的 proposer 列表：

```
function selectProposers(epoch e):
    seed = VRF(prevEpochSeed, e)
    proposers = []
    for slot s in [e*32, (e+1)*32):
        // 按 stake 加权随机
        proposers[s] = weighted_random(V_active, stake_weights, seed, s)
    return proposers
```

公开预计算便于 V 节点提前进入热备状态，降低出块延迟。

### 2.4 V 节点准入与退出

#### 加入

```
JoinV(addr A):
    require balanceOf(A) ≥ S_min
    require A 注册 BLS pubkey + VRF pubkey
    transfer S_min from A to VNodeStaking 合约 (锁定)
    insert A into V_pending
    after T_warmup -> V_active 加入下一 epoch
```

#### 退出

```
LeaveV(addr A):
    require A ∈ V_active
    move A.stake from V_active to V_cooling
    unfreeze after T_unstake 天，期间无 slashing 事件即解锁返还
```

#### 抵押要求

| 参数 | 默认值 | 备注 |
|---|---|---|
| `S_min(V)`（最少抵押） | `《[占位] 50,000》` AII | 治理可调 |
| `S_max(V)`（单节点权重上限） | `《[占位] 200,000》` AII | 防止单节点过度集中 |
| V 节点数量上限 | 101 | 可治理 21–301 |
| `T_warmup`（预热期） | 1 epoch | 防止抵押后立即出块（无安全审查时间） |
| `T_unstake`（冷却期） | 14 天 | 防止退出后回头作恶（长程攻击） |
| `Reward(V)`（年化目标） | 6–10% | 含 proposer 与 attester 奖励 |

### 2.5 V 节点罚没（Slashing）

| 触发事件 | 罚没比例 | 备注 |
|---|---|---|
| 双签（同 slot 签两个不同区块的 PRE-COMMIT） | **100% 抵押** | 任何节点可提交两份签名为证 |
| 反向投票（先 PRE-VOTE B，后又 PRE-VOTE B'） | 部分罚没（与作恶面成正比） | – |
| 缺席 ≥ 20% epoch slots 投票 | 0.5% 抵押 / epoch | – |
| 连续 5 epoch 完全离线 | 永久除名 + 100% 罚没 | – |
| 提交无效 Flush / 跨链证明 | 10% 抵押 | – |

罚没金分配：

- 50% 销毁
- 30% 进入举报者奖励池
- 20% 进入安全保险基金

### 2.6 单区块最终性的安全前提

BFT 安全性证明（详见《09 安全与威胁模型》§3）的关键前提：

- 同步假设：网络在合理延迟内（≤ 6 秒 slot 时间）传播消息
- 拜占庭容忍：≤ ⅓ stake 作恶
- 弱主观性：新加入节点须从受信任检查点同步（防长程攻击）

### 2.7 不再有"矿工"角色

AII 主链**没有 PoW 矿工**。区块奖励完全归属于：

- **Proposer**（当 slot 出块者）
- **Attesters**（PRE-VOTE / PRE-COMMIT 参与的 V 节点）
- **DAO 国库**

详细奖励分配见《05 代币经济与分配模型》§4。

---

## 3. 子链共识引擎接口

详见《03 架构设计文档》§5.1。每种共识算法需实现该接口并注册。下面逐一展开默认提供的四种内置共识。

---

## 4. 子链共识算法

### 4.1 PoS（默认）

#### 4.1.1 选举

每个 epoch（默认 `《[占位] 6》` 小时），按 stake 加权随机选出当 epoch 出块者列表：

```
seed = H(prev_epoch_seed || prev_epoch_finalHash)
for each slot in epoch:
    proposer = weighted_random_pick(all_validators, stake_weights, slot, seed)
```

#### 4.1.2 出块与确认

```
slot_time = 2 秒（可治理）
每个 slot 由 proposer 提议区块
每个 attester（验证人）对区块签名
当区块收到 ≥ ⅔ stake 加权签名 -> attested
当被后续 N=64 个区块再次 attest -> finalized
```

#### 4.1.3 PoS 罚没

| 事件 | 罚没 |
|---|---|
| 双签 | 100% |
| 反向投票（vote 之前确认的区块）| 部分罚没（与作恶面成正比） |
| 长时间离线（>20% epoch slots） | 0.5% / epoch |

#### 4.1.4 PoS 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `S_min(PoS)` | `《[占位] 1,000》` 代币 | 单 SCS 入门门槛 |
| Slot 时间 | 2 秒 | – |
| Epoch 长度 | 10800 slots（6 小时） | – |
| 最终性窗口 | 2 epochs | – |
| 提议者数 | ≥ 64 | – |

### 4.2 PBFT（联盟/金融子链）

#### 4.2.1 算法

经典 PBFT 三阶段：`Pre-Prepare → Prepare → Commit`。

```
节点数 n，恶意节点上限 f = (n-1)/3
区块由轮值 leader 提议
每轮三阶段完成后区块即最终
view-change：leader 超时未提议 -> 触发更换 leader
```

#### 4.2.2 BLS 聚合签名优化

每阶段的签名集合用 BLS12-381 聚合，单签名验证开销 O(1)。

#### 4.2.3 PBFT 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `n`（节点数） | 7 / 21 / 51 | 可治理 |
| `view_timeout` | 6 秒 | leader 超时 |
| 区块时间 | 1–3 秒 | – |
| 最终性 | 单区块 | – |
| `S_min(PBFT)` | `《[占位] 5,000》` 代币 | – |

### 4.3 DPoS（高 TPS 游戏）

#### 4.3.1 见证人选举

```
全网代币持有者投票选出 21 名见证人（数字可治理）。
每 21 个 slot 一轮，每位见证人按确定顺序产 1 块。
slot 时间默认 0.5 秒，1 轮 10.5 秒。
```

#### 4.3.2 缺席处罚

连续缺席 ≥ 3 次自动从见证人池剔除，由备选见证人补位。

#### 4.3.3 DPoS 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| 见证人数 | 21 | 可治理 1–101 |
| Slot 时间 | 0.5 秒 | – |
| 投票冷却 | 24 小时 | 防止快速切换 |
| 最终性 | 单区块（弱）；2/3 见证人确认（强） | – |

### 4.4 Custom（自定义）

子链发起方可上传自己的共识引擎 WASM 二进制（或注册到主链合约的引擎 ID）。审核机制：

- 必须通过协议级共识引擎兼容性测试（CETF：Consensus Engine Test Framework）
- 必须通过AII DAO安全审计
- 必须有 ≥ S_min(Custom) = `《[占位] 100,000》` 代币抵押作为质量背书

---

## 5. V 节点 ↔ SCS 节点协调

### 5.1 子链调度

每条子链对应一个由 V 节点抽签产生的**协调者（Coordinator）**：

- 协调者每个 epoch 重新抽签
- 职责：
  - 监听该子链的状态与共识进度
  - 在 Flush 周期提交 Flush 交易到主链
  - 触发 SCS 节点出错时的应急处理（请求其他 V 节点见证、提交罚没证据）

### 5.2 Flush 流程

```
每 T_flush 个主链块（默认 30，≈ 5 分钟），协调者：

1. 拉取子链最新区块 B_s（要求已达到子链最终性）
2. 拉取 B_s 包含的所有共识证明 σ_proof
3. 构造 Flush 交易：
   Flush = {
       microChainId,
       fromHeight = lastFlush.toHeight + 1,
       toHeight   = B_s.height,
       stateRoot  = B_s.stateRoot_s,
       receiptsRoot = B_s.receiptsRoot_s,
       scsSet     = currentSCSValidators,
       proof      = σ_proof,
   }
4. 提交 Flush 交易到主链
5. 主链全局合约 MotherChainRegistry 验证 proof，写入子链注册表
6. 触发 SCS 节点奖励发放事件
```

### 5.3 异常处理

| 情况 | 处理 |
|---|---|
| 子链 stalled（>2×Flush周期无新块） | 协调者上报，触发子链状态调查；V 节点投票决定是否暂停子链 |
| 子链双链分叉 | 协调者选择具有更高经济权重的链；若难以决断，提交治理仲裁 |
| 协调者本身宕机 | 其他 V 节点检测到协调者无响应，超时后选举新协调者 |
| Flush 证明无效 | 提交者被罚没 |

---

## 6. SCS 节点准入

SCS 节点参与某条子链需要：

1. 抵押：≥ 该子链定义的 `S_min(C)`
2. 共识身份验证：BLS / VRF 公钥注册到 `MotherChainRegistry`
3. 子链发起方批准（实验级子链可选）

SCS 节点退出冷却期：默认 7 天。

---

## 7. 跨子链消息共识保障

跨链消息从源子链到目的子链的端到端流程涉及 4 个共识点：

1. **源子链共识** → 消息事件被源子链共识确认
2. **源子链 Flush** → 主链共识确认事件已经发生
3. **目的子链协调者** → 监听主链事件
4. **目的子链共识** → 消息被纳入目的子链区块

每一步都有相应的共识证明可链上验证。

---

## 8. 主链共识长期演进

AII 主网启动即为**纯 PoS BFT**，无需中途切换。后续可能的演进方向：

| 演进方向 | 触发条件 | 备注 |
|---|---|---|
| 加大 V 节点上限至 301+ | TPS / 去中心化需求 | 共识算法本身不变 |
| 引入零知识证明加速 | ZK 工具成熟 | 用 ZK 证明压缩 PRE-COMMIT 集合 |
| 跨链锚定到 BTC / ETH | 安全增强 | 周期性提交 stateRoot 上 BTC，增强抗 51% 攻击 |
| 切换至更新 BFT 算法（如 HotStuff-2）| 性能优化 | 治理决定，需 ≥ 6 月稳定测试网 |

> **差异化决策**：相对 MOAC 原方案的 PoW + 子链可插拔共识，AII 将主链改为纯 PoS BFT。理由：(1) 单区块即时最终性大幅简化跨链证明；(2) 无 PoW 消耗与硬件门槛；(3) 与以太坊 The Merge 后的行业方向一致；(4) 子链可插拔共识保留不变。

---

## 9. 共识安全证明思路（待形式化）

### 9.1 BFT-PoS 的攻击成本

设 V 节点抵押总值 S（以代币计），代币单价 P_token：

```
Cost_attack ≥ ⅓ × S × P_token （攻击活性，让链停滞）
Cost_attack ≥ ⅔ × S × P_token （攻击安全性，使链分叉/双花，且自身抵押全部罚没）
```

由于双签 100% 罚没，攻击者一旦发起安全攻击立刻损失 **⅔ × S × P_token**（攻击者的全部抵押），这种"破坏即自毁"的经济模型相对纯 PoW 提供**更明确的成本下限**。

### 9.2 PBFT 子链的 ⅓ 容错

经典 PBFT 证明：当 f ≤ (n-1)/3 时，系统满足安全性（不会同时确认两个矛盾区块）与活性（在网络同步期内能持续出块）。

### 9.3 长程攻击防护

子链区块头携带 `anchorMotherBlock`，记录子链锚定的主链高度。任何"穿越历史"的子链分叉必须能产出一条更长且锚定更早主链的链，但由于主链 PoW 不可篡改，攻击不成立。

---

## 9.5 节点分级（Node Tiers）与硬件门槛

AII 采用**按硬件能力分级**的节点模型——任何人都可参与，但参与方式与协议责任由设备能力决定。共有 7 个 Tier：

| Tier | 节点角色 | 共识参与 | 推荐 CPU | 推荐 RAM | 推荐磁盘 | 推荐带宽 | 抵押要求 |
|---|---|---|---|---|---|---|---|
| **T1** | V 节点（Validator） | 主链 PoS BFT 出块/投票 + 子链调度 | 16+ 核 | 64+ GB | 2+ TB NVMe | 1 Gbps 对称 | ≥ 50,000 AII |
| **T2** | SCS 节点 | 子链共识 + 合约执行 | 8 核 | 32 GB | 1 TB NVMe | 100 Mbps | 由子链定义 |
| **T3** | 归档全节点 | 仅验证（不出块） | 8 核 | 32 GB | 5+ TB SSD | 100 Mbps | 无 |
| **T4** | 全节点 | 仅验证 | 4 核 | 16 GB | 1 TB SSD | 100 Mbps | 无 |
| **T5** | 修剪节点（Pruned） | 仅验证近期状态 | 2 核 | 8 GB | 500 GB SSD | 50 Mbps | 无 |
| **T6** | 轻节点（Light） | 区块头同步 + 按需 Merkle 证明 | 1 核 | 2 GB | 50 GB | 5 Mbps | 无 |
| **T7** | 移动钱包（SPV）| 仅签名 + RPC 调用 | 手机 SoC | 0.5 GB | 100 MB | 4G/Wi-Fi | 无 |

### 9.5.1 关键概念

- **T1–T2 = 共识贡献者**：参与协议级出块/验证，须抵押，获得区块奖励。
- **T3–T5 = 数据贡献者**：保留完整或部分链状态，为网络提供 RPC/查询能力。无强制奖励，但可参与社区运营的 RPC 激励计划。
- **T6–T7 = 用户节点**：消费链上服务，不向网络贡献存储/算力。

### 9.5.2 客户端自动识别逻辑

官方客户端（详见《03 架构设计文档》§14）在首次启动时执行以下检测：

```text
1. 探测硬件（基于 Rust crate `sysinfo`）：
   - CPU 核心数（逻辑核 + 物理核）
   - RAM 总量
   - 主磁盘剩余空间 + 磁盘类型（HDD / SATA SSD / NVMe）
   - 磁盘随机 4KB IOPS（运行 10 秒 fio-like 测试）
2. 探测网络：
   - 公网 IP 是否可达（NAT 类型 + UPnP 可用性）
   - 至 3 个 bootstrap 节点的平均延迟与可持续带宽
3. 评分（0–100）：
   score_cpu      = clamp(cores / 16, 0, 1) * 25
   score_ram      = clamp(ram_gb / 64, 0, 1) * 25
   score_disk     = clamp(disk_tb / 2, 0, 1) * 25 * (nvme ? 1.0 : 0.6)
   score_network  = clamp(bw_mbps / 1000, 0, 1) * 25 * (public_ip ? 1.0 : 0.5)
   total = score_cpu + score_ram + score_disk + score_network
4. 推荐 Tier：
   total ≥ 85  →  T1（V 节点候选）
   total ≥ 65  →  T2（SCS 节点候选）
   total ≥ 50  →  T3（归档全节点）
   total ≥ 35  →  T4（全节点）
   total ≥ 20  →  T5（修剪节点）
   total ≥ 10  →  T6（轻节点）
   else        →  T7（移动钱包）
```

### 9.5.3 用户确认流程

客户端首启界面展示：

```
✅ 检测到的硬件配置
   CPU      : 16 核 @ 3.6 GHz
   RAM      : 64 GB DDR5
   磁盘     : 2 TB NVMe (剩余 1.6 TB) - 随机读 18K IOPS
   网络     : 公网 IP ✓ | 上行 800 Mbps | 至 bootstrap 35ms

📊 综合评分：91 / 100

🎯 推荐角色：T1 - V 节点（Validator）
   预期年化奖励：8–15%
   抵押要求    ：50,000 AII
   预计同步    ：14 小时
   预计磁盘占用：~600 GB（含归档）

[继续以 T1 启动] [降级为 T2] [降级为 T3 / T4 / T5 / T6] [自定义]
```

### 9.5.4 动态降级与升级

- 节点运行期间若硬件指标持续 24h 跌出当前 Tier 下限（如磁盘满、网络降级），客户端**自动告警**并提示降级，避免节点被罚没。
- 升级硬件后客户端可重新评分；从低 Tier 升至 V/SCS 节点需额外通过抵押与社区流程。

### 9.5.5 移动端特殊处理（T6/T7）

- iOS / Android 客户端只承担 T6 或 T7 角色
- 不参与共识、不存储完整链状态
- 通过远程 RPC 节点（社区运营或自建）获取数据
- 默认通过 P2P 走 libp2p WebSocket，移动网络下走 HTTPS RPC

> **设计哲学**：AII 不设"硬件门槛准入"——任何配置都能参与，但**参与协议责任的资格**与硬件能力挂钩。这平衡了去中心化与性能：小设备做用户，大设备做基础设施。

---

## 10. 共识可治理参数清单

下列参数都通过链上治理可调（详见《07 治理与社区架构》§4）：

| 参数 | 默认 | 调整窗口 |
|---|---|---|
| V 节点最大数量 | 101 | 21–301 |
| `S_min(V)` | 50,000 AII | ±50% |
| `T_warmup` | 1 epoch | 1–8 epochs |
| `T_unstake` | 14 天 | 7–28 天 |
| `T_flush` | 32 slots（1 epoch） | 16–96 slots |
| 主链 slot 时间 | 6 秒 | 3–12 秒 |
| epoch 长度 | 32 slots | 16–128 slots |
| 子链 PoS slot 时间 | 2 秒 | 1–10 秒 |
| 主链 gasLimit | 30M | 由协议自动 ±1/1024 |
| 双签罚没 | 100% | 不可调（协议级） |
| 区块奖励初始值 | 2 AII | 治理可调，年内调整次数 ≤ 1 |

---

## 本批次占位字段汇总

| 占位符 | 出现位置 | 建议候选 |
|---|---|---|
| `《[占位] 50,000》` | §2.4 V 节点抵押 | 与黄皮书一致 |
| `《[占位] 200,000》` | §2.4 V 节点权重上限 | – |
| `《[占位] 6》`小时 | §4.1 PoS epoch | – |
| `《[占位] 1,000》` | §4.1 PoS 入门 | – |
| `《[占位] 5,000》` | §4.2 PBFT 入门 | – |
| `《[占位] 100,000》` | §4.4 Custom 共识 | – |

— 本共识机制设计完 —
