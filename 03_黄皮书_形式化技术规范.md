# 03 AII 黄皮书 — 形式化技术规范

> 版本：v0.1（初稿）
> 适用对象：协议工程师、共识算法实现者、第三方节点客户端实现方、密码学审计方
> 关联：本黄皮书定义协议层的精确数据结构、状态转移函数与共识规则；高层愿景见《01 技术白皮书》；模块组织见《04 架构设计文档》。

---

## 0. 记号与基础定义

### 0.1 类型记号

- `𝔹`：bit 集，`{0, 1}`。`𝔹ⁿ` 表示 n 位的字节串。
- `𝔹*`：任意长度字节串集。
- `ℕ`：非负整数。`ℕ₂₅₆` 表示 [0, 2²⁵⁶) 区间。
- `Σ`：世界状态（World State），从 20 字节地址映射到账户状态的集合。
- `⊥`：空值 / 未定义。
- `H(x)`：Keccak-256 哈希函数，`H : 𝔹* → 𝔹²⁵⁶`。
- `RLP(x)`：递归长度前缀编码，与以太坊定义一致。

### 0.2 时间单位

- 主链 slot 时间记为 `Tₘ`，目标值 `《[占位] 6》` 秒。每 slot 至多产 1 个区块（BFT 共识，无 uncle）。
- epoch 长度 `E_len = 32 slots`（约 3 分钟）。
- 子链出块时间由子链自身共识参数决定，记为 `Tₛ(c)`，其中 `c` 为子链 ID。
- 主链刷新周期 `T_flush`，默认 1 epoch（32 主链 slot）。

---

## 1. 账户模型

### 1.1 账户类型

本链沿用以太坊式账户模型，账户分为两类：

- **外部账户（EOA, Externally Owned Account）**：由 secp256k1 私钥控制，无代码。
- **合约账户（CA, Contract Account）**：由代码控制，不存私钥，地址由部署者地址 + nonce 计算得出。

地址生成：
```
addr_EOA = last_20_bytes( H(pubkey_uncompressed[1:]) )
addr_CA  = last_20_bytes( H(RLP([deployer_addr, nonce])) )
```

### 1.2 账户状态

每个账户的状态 `σ[a]` 为四元组：

```
σ[a] = (nonce, balance, storageRoot, codeHash)
```

| 字段 | 类型 | 含义 |
|---|---|---|
| `nonce` | ℕ₂₅₆ | EOA：发出交易计数；CA：创建合约计数 |
| `balance` | ℕ₂₅₆ | 账户原生代币余额，以 wei 为单位 |
| `storageRoot` | 𝔹²⁵⁶ | 该账户存储 MPT 根哈希 |
| `codeHash` | 𝔹²⁵⁶ | 账户代码的 Keccak-256；EOA 为 `H("") = 0xc5d2…ca70` |

### 1.3 世界状态

世界状态 `σ` 是地址到账户状态的映射，承载于一棵全局默克尔帕特里夏树（State MPT）。该 MPT 根哈希 `stateRoot` 写入每个主链区块头。

子链有独立的世界状态 `σ_c`，其根定期通过 Flush 操作写入主链。

---

## 2. 交易结构

### 2.1 主链交易

主链交易 `T_m` 的 RLP 编码字段：

```
T_m := (nonce, gasPrice, gasLimit, to, value, data, chainId, v, r, s)
```

| 字段 | 类型 | 说明 |
|---|---|---|
| nonce | ℕ₂₅₆ | 发送者当前 nonce |
| gasPrice | ℕ₂₅₆ | 每单位 gas 价格（wei） |
| gasLimit | ℕ₂₅₆ | 本交易允许消耗 gas 上限 |
| to | 𝔹²⁰ 或 ⊥ | 接收者地址；⊥ 表示合约创建 |
| value | ℕ₂₅₆ | 转账金额（wei） |
| data | 𝔹* | 合约调用 calldata 或合约创建初始化代码 |
| chainId | ℕ | 本链主链 ID，详见 §8 |
| v, r, s | – | secp256k1 签名（EIP-155 风格） |

交易哈希：`txHash = H(RLP(T_m))`。

### 2.2 子链交易

子链交易 `T_s` 比主链交易多两个字段：

```
T_s := (microChainId, ..., T_m_fields..., scsSignature)
```

| 新增字段 | 类型 | 说明 |
|---|---|---|
| microChainId | 𝔹²⁰ | 目标子链 ID（即子链注册合约地址） |
| scsSignature | 𝔹⁶⁵ | 由该子链当前出块 SCS 节点附加的二次签名（部分共识算法可省略） |

子链交易在子链本地确认即生效；在 Flush 时其执行收据根作为整体写入主链。

### 2.3 跨链交易

跨链交易是一种特殊的协议级交易，由源链生成、目的链消费：

```
T_cross := (srcChainId, dstChainId, srcAddr, dstAddr, asset, amount, nonce, proof, signature)
```

`proof` 为源链区块头 + 默克尔包含证明，目的链通过验证 `proof` 释放对应资产。详见 §9。

---

## 3. 区块结构

### 3.1 主链区块

```
Block_m := (header, transactionsList, uncleList, flushList)
```

主链区块头 `header` 字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| parentHash | 𝔹²⁵⁶ | 父区块哈希 |
| slot | ℕ₆₄ | 本区块所属 slot 号（每 slot 至多 1 块） |
| epoch | ℕ₆₄ | 所属 epoch（= slot / 32） |
| proposer | 𝔹²⁰ | 出块 V 节点地址（由 VRF 选出） |
| stateRoot | 𝔹²⁵⁶ | 区块执行后的世界状态根 |
| transactionsRoot | 𝔹²⁵⁶ | 交易 MPT 根 |
| receiptsRoot | 𝔹²⁵⁶ | 收据 MPT 根 |
| flushRoot | 𝔹²⁵⁶ | 本区块包含的子链 Flush 列表的 MPT 根 |
| logsBloom | 𝔹²⁵⁶ | 收据 logs 的 Bloom 过滤器 |
| number | ℕ₂₅₆ | 区块高度 |
| gasLimit | ℕ₂₅₆ | 主链 gas 上限 |
| gasUsed | ℕ₂₅₆ | 实际消耗 gas |
| timestamp | ℕ₆₄ | Unix 时间戳 |
| extraData | 𝔹⁰⁻³² | 提议者自定义数据 |
| randaoMix | 𝔹²⁵⁶ | 本 epoch RANDAO 累积熵（基于 VRF 输出） |
| parentCommitSig | 𝔹⁹⁶ | 父区块的 BFT 终结签名（聚合 BLS，全体 PRE-COMMIT 签名 ⅔+ 阈值） |
| parentCommitBitmap | 𝔹\* | 标识 parentCommitSig 中包含哪些 V 节点的位图 |
| proposerSig | 𝔹⁹⁶ | proposer 对前述全部字段的 BLS 签名 |

> **差异化决策**：相对以太坊主网，本链区块头：
> - **删除** difficulty / mixHash / nonce / ommersHash（PoW 字段不复存在）
> - **新增** slot / epoch / proposer / randaoMix（PoS 时间结构与提议者）
> - **新增** parentCommitSig / parentCommitBitmap（聚合 BFT 终结签名，承载父区块的最终性证书）
> - **新增** flushRoot（承载子链最终性，与共识机制无关）

### 3.2 子链区块

```
Block_s := (header_s, transactionsList_s)
```

子链区块头字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| parentHash | 𝔹²⁵⁶ | 子链父区块 |
| microChainId | 𝔹²⁰ | 所属子链 ID |
| consensusType | uint8 | 共识算法标识：1=PoS, 2=PBFT, 3=DPoS, 4=Custom |
| proposer | 𝔹²⁰ | 出块 SCS 节点 |
| stateRoot_s | 𝔹²⁵⁶ | 子链本地状态根 |
| transactionsRoot_s | 𝔹²⁵⁶ | 交易 MPT 根 |
| number_s | ℕ₂₅₆ | 子链区块高度 |
| timestamp | ℕ₂⁵⁶ | Unix 时间戳 |
| consensusProof | 𝔹* | 共识证明（PoS：签名集合；PBFT：commit 证书；…） |
| anchorMotherBlock | ℕ₂₅₆ | 最近一次锚定的主链高度（防长程攻击） |

### 3.3 Flush（子链 → 主链锚定）

Flush 操作是一笔特殊的主链交易，由 V 节点提交：

```
Flush := (microChainId, fromBlock_s, toBlock_s, stateRoot_s_after, receiptsRoot_s, scsSet, proof)
```

主链全局合约 `MotherChainRegistry` 验证 `proof`（共识证明 + SCS 节点签名集合）后，将 `stateRoot_s_after` 写入子链注册表，并向 SCS 节点分发奖励。

---

## 4. 共识

### 4.1 主链共识：BFT-PoS

主链每个 slot 至多产生 1 个有效区块 `B_s`，必须同时满足：

1. **Proposer 条件**：`B_s.proposer = VRF(epoch_seed, V_active, slot s)`，由本 epoch 预计算确定。
2. **Proposer 签名**：`proposerSig` 是 `proposer` 对 `H(RLP(header without proposerSig))` 的有效 BLS 签名。
3. **父最终性证书**：`B_s.parentCommitSig` 是 ≥ ⅔ stake 加权的聚合 BLS 签名，证明父区块 `B_{s-k}` 已被全网 finalize。
4. **状态转移正确**：交易批量执行后的 stateRoot / receiptsRoot 与 header 字段一致。

V 节点集合 `V_active` 由协议级 `VNodeStaking` 合约维护：

- 加入：抵押 ≥ `S_min(V)`（默认 `《[占位] 50000》` AII），过 `T_warmup` = 1 epoch 后生效
- 退出：发起 unstake 后冷却 `T_unstake` = `《[占位] 14》` 天
- 罚没：双签、反向投票、长期离线触发 slashing（详见 §4.5）

> **差异化决策**：相对 MOAC 原方案（纯 PoW 主链），本链采用**纯 PoS BFT** + 单区块即时最终性。无 PoW、无矿工、无算力竞争。安全完全由经济抵押与 BFT ⅔ 拜占庭容错保证。

### 4.2 BFT 投票流程

```
slot s:
  1. proposer 广播 B_s
  2. V_active 中所有节点验证 B_s
  3. 验证通过的节点广播 PRE-VOTE(slot s, hash(B_s))
  4. 节点收集 PRE-VOTE 至 ≥ ⅔ stake → 广播 PRE-COMMIT(slot s, hash(B_s))
  5. 节点收集 PRE-COMMIT 至 ≥ ⅔ stake → 将 B_s 标记为 FINALIZED
  6. 下一 slot B_{s+1} 的 parentCommitSig 携带聚合 PRE-COMMIT
```

PRE-VOTE 与 PRE-COMMIT 消息使用 BLS 签名，可在 §7.2 BLS12-381 上聚合验证（O(1) 时间验证 N 个签名）。

### 4.3 子链可插拔共识

每条子链在创建时指定 `consensusType`：

| 类型 ID | 名称 | 关键参数 | 适用场景 |
|---|---|---|---|
| 1 | PoS | stake_min, slot_time | 通用 |
| 2 | PBFT | n, f, view_timeout | 联盟 / 金融 |
| 3 | DPoS | num_witnesses, round_time | 高 TPS 游戏 |
| 4 | Custom | 自定义 ABI 接口 | 实验性 |

详细参数与算法伪代码见《05 共识机制详细设计》。

### 4.4 区块最终性

- **主链**：**单区块即时最终性**——当 B_s 收集到 ⅔ stake 加权 PRE-COMMIT 即视为 FINALIZED（同 slot 内）。无重组、无概率最终。
- **子链**：根据共识类型决定。PBFT/DPoS 单区块即最终；PoS 需经过 `《[占位] 3》` 个 epoch 确认。

### 4.5 罚没规则（Slashing）

| 触发事件 | 适用对象 | 罚没比例 | 备注 |
|---|---|---|---|
| 双签（同 slot 签两个不同区块的 PRE-COMMIT） | V 节点 / SCS 节点 | 100% 抵押 | 由任何节点提交证据触发 |
| 反向投票（PRE-VOTE 与已发送的 PRE-VOTE 冲突） | V 节点 | 部分罚没 | 与作恶面成正比 |
| 缺席 ≥ 20% epoch slots | V 节点 | 0.5% 抵押 / epoch | – |
| 缺席 Flush（V 节点） | V 节点 | 0.5% 抵押 / 次 | 三次累计转为永久除名 |
| 缺席出块（SCS PBFT/DPoS） | SCS 节点 | 0.1% 抵押 / 次 | 子链 governance 可调 |
| 提交无效跨链证明 | V 节点 | 10% 抵押 | 一次性 |

罚没金的 50% 销毁，50% 进入举报者奖励池。

---

## 5. 状态转移函数

### 5.1 全局形式化定义

世界状态 `σ` 由区块 `B` 推进：

```
σ' = Υ(σ, B) = Φ(σ_n, R_finalization)

σ_0 = σ
σ_{i+1} = Υ_T(σ_i, T_i)   for i in 0..n-1
σ_n = σ after all transactions
```

其中：
- `Υ_T` 是单笔交易的状态转移函数（同以太坊语义）。
- `Φ` 是区块终结函数（应用区块奖励、扣 gas、收 Flush 等）。

### 5.2 单笔交易状态转移 `Υ_T`

输入：当前状态 `σ`，交易 `T`。

```
Υ_T(σ, T) → σ'
```

执行步骤：
1. 验证交易合法性（签名、nonce、余额、gasLimit ≤ blockGasLimit）。
2. 从发送者扣除 `gasLimit × gasPrice`。
3. 发送者 nonce += 1。
4. 若 `T.to == ⊥`：创建合约，运行 init code。
5. 否则：执行 `T.to` 处的合约代码（若为 EOA 则仅转账）。
6. 剩余 gas 退还发送者；消耗 gas 计入区块。
7. 写入收据 `R` 到 receiptsRoot。

### 5.3 合约创建地址

```
addr_new = last_20_bytes( H(RLP([sender, sender.nonce - 1])) )
```

CREATE2 操作码使用：

```
addr_new = last_20_bytes( H(0xff || sender || salt || H(init_code)) )
```

---

## 6. Gas 计量

### 6.1 Gas 表（与以太坊 Shanghai 版兼容）

| 操作 | Gas | 说明 |
|---|---|---|
| 基础交易 | 21000 | 任何非合约创建交易 |
| 合约创建 | 32000 | 加上 `data` 中每非零字节 16、零字节 4 |
| SSTORE（新槽） | 22100 | 写入此前为空的存储槽 |
| SSTORE（修改） | 5000 | 修改非零槽 |
| SLOAD | 2100 | 读取存储槽（cold） |
| CALL | 700 起 | 不含转账与新账户创建 |
| BALANCE | 700 | cold；warm 100 |
| CREATE | 32000 | – |
| LOG0…LOG4 | 375 + 8/byte | 加 topic 数 × 375 |

完整 gas 表与 EIP-2929 / EIP-3529 调整保持一致。子链合约执行**不消耗主链 gas**，子链自行决定计量方式（详见《04 架构设计文档》§4.5）。

### 6.2 BlockGasLimit 调整

```
gasLimit_{B+1} = clamp(
    gasLimit_B - gasLimit_B // 1024,
    gasLimit_B + gasLimit_B // 1024,
    desired_value
)
```

`desired_value` 是协议常量（不可治理），固定为 `30,000,000`。任何变更须 hard fork。

---

## 7. 密码学原语

### 7.1 哈希

- **Keccak-256**：默认哈希（FIPS 202 草案版，与以太坊保持一致；与 SHA3-256 不同）。
- **SHA-256**：跨链与 BTC 兼容场景使用。
- **Blake2b-256**：可选，用于轻量子链。

### 7.2 签名

- **主签名**：secp256k1 ECDSA，输出 `(v, r, s)`，65 字节。
- **聚合签名**：BLS12-381，用于 PBFT/PoS 子链的聚合签名（详见《05》§4）。
- **门限签名**：基于 BLS，用于跨链多签桥。

### 7.3 默克尔结构

- **MPT（Merkle Patricia Trie）**：账户状态、存储、收据使用。
- **Binary Merkle Tree**：交易列表使用。
- **Verkle Tree**：路线图后期阶段（Phase 5+）考虑替换状态树（详见《10》）。

---

## 8. 链 ID 与网络 ID

- **主链 chainId**：`《[占位] 99》`（避免与以太坊主网 1、BSC 56、Polygon 137 等冲突；待审定）
- **网络 ID**：与 chainId 相同
- **子链 chainId**：`microChainId` 字段（20 字节地址直接作为子链 ID，避免子链 ID 命名空间冲突）

签名时使用 chainId 作为 EIP-155 防重放标记。跨链交易额外携带源 / 目标 chainId。

---

## 9. 跨链协议

### 9.1 内部跨链（子链 ↔ 子链）

源子链合约调用 `precompile.CrossChainSend(targetChain, targetContract, payload, value)`：

1. 该调用消耗源子链 gas，产生跨链事件。
2. 在下一次 Flush 时，跨链事件随 `flushRoot` 一同写入主链。
3. 主链 `CrossChainRouter` 全局合约根据 `targetChain` 投递事件。
4. 目标子链 V 节点观察主链事件，将其包装为目标子链交易并广播给目标 SCS 节点。
5. 目标子链处理后，结果回写并最终通过下一次 Flush 反馈给主链。

整个过程通过主链作为信任根，**不需要任何第三方桥**。

### 9.2 外部跨链

#### 9.2.1 与 BTC：HTLC 原子交换

Alice 持有本链代币，Bob 持有 BTC，双方约定哈希预映像 `s`：

1. Alice 在本链锁定金额 X 至 HTLC 合约，条件：知道 `H(s)` 的预映像 `s` 可领取，超时后退回。
2. Bob 在 BTC 链构造 HTLC，条件：知道 `s` 可领取，超时后退回。
3. Alice 通过 `s` 领取 BTC，过程暴露 `s`。
4. Bob 通过 `s` 领取本链代币。

#### 9.2.2 与 ETH：双向跨链桥

通过部署在两条链上的镜像合约 + 验证者多签实现（详见《04 架构设计文档》§8.3）。

#### 9.2.3 与 Cosmos / Polkadot：IBC / XCM 适配

实现 IBC 标准接口（ICS-002 / ICS-003 / ICS-004 / ICS-020）作为本链的"对外通信网卡"。

---

## 10. 虚拟机规范

### 10.1 主链 VM：EVM 完全兼容

本链主链虚拟机实现完整 EVM 指令集（截至以太坊 Shanghai 升级，包含 PUSH0、EIP-3651、EIP-3855、EIP-3860 等）。后续升级跟随以太坊节奏。

### 10.2 子链 VM：可选

子链发起方在创建子链时指定 VM 类型：

| VM 类型 | 标识 | 说明 |
|---|---|---|
| EVM | 0x01 | 默认 |
| WASM | 0x02 | 基于 Wasmtime / Wasmer 的子集，支持 Rust/AssemblyScript |
| Move | 0x03 | 路线图后期支持 |
| ZK-VM | 0x04 | 路线图后期支持 |

### 10.3 预编译合约扩展

除以太坊标准预编译外，本链额外提供：

| 地址 | 功能 | 输入 / 输出 |
|---|---|---|
| 0x0a | BLS12-381 配对验证 | – |
| 0x0b | 跨链消息发送 | `CrossChainSend` |
| 0x0c | 子链状态查询 | 返回某子链当前 stateRoot |
| 0x0d | V 节点签名验证 | – |
| 0x0e | IBC 包验证 | – |

---

## 11. RPC 接口（最小集）

主链 JSON-RPC 接口与以太坊保持兼容，包括：

- `eth_blockNumber`, `eth_getBalance`, `eth_getBlockByHash`, `eth_sendRawTransaction`, `eth_call`, `eth_getLogs`, `eth_subscribe` …
- 本链扩展前缀 `mc_`：
  - `mc_listMicroChains` — 枚举所有活跃子链
  - `mc_getMicroChainStateRoot(chainId, blockNum)` — 查询特定子链状态
  - `mc_subscribeFlush(chainId)` — 订阅子链 Flush 事件
  - `mc_getCrossChainProof(srcTx)` — 获取跨链事件证明
  - `mc_getVNodeSet(epoch)` — 查询给定 epoch 的 V 节点集

子链节点提供以子链为根的相同接口集，但 `eth_chainId` 返回 `microChainId`。

---

## 12. 序列化与编码

### 12.1 RLP

所有链上结构（区块、交易、收据、状态节点）使用 RLP 编码，与以太坊 RLP 规范完全一致。

### 12.2 ABI

合约调用 calldata 使用 Solidity ABI v2，与以太坊一致。

### 12.3 二进制兼容性承诺

本链承诺：

- 主链交易签名格式与以太坊**完全兼容**（同私钥可签名两链交易，依靠 chainId 防重放）。
- 主链账户地址与以太坊**完全兼容**（同公钥推导同地址）。
- 主链 RPC 协议与以太坊**完全兼容**（钱包/SDK 仅需更换 RPC 端点）。

---

## 13. 协议参数表

所有参数为**协议常量**（hard-coded constant），变更须 hard fork。**无链上治理可调参数**。

| 参数 | 符号 | 协议常量值 |
|---|---|---|
| 主链 slot 时间 | Tₘ | 6 秒 |
| epoch 长度 | E_len | 32 slots（192 秒） |
| 主链区块 gas 上限 | gasLimit_m | 30,000,000（自动 ±1/1024 范围内浮动） |
| V 节点最少抵押 | S_min(V) | **50 AII**（已下调以适配预约启动模型） |
| V 节点单节点权重上限 | S_max(V) | 5,000 AII |
| V 节点数量软上限 | N_max(V) | 101 |
| V 节点预热期 | T_warmup | 1 epoch |
| V 节点冷却期 | T_unstake | 14 天 |
| Flush 周期 | T_flush | 32 slots（1 epoch） |
| 主链最终性 | – | 单区块（BFT） |
| 双签罚没 | – | 100% 抵押 |
| BFT 阈值 | – | ⅔ stake |
| 主链 chainId | – | 99（占位，最终值由 genesis 确定）|
| 区块奖励初始 | R_init | 2 AII |
| 区块奖励减半周期 | – | 每 4 年 |
| 区块奖励分配 | – | 80% 提议者 / 20% PRE-COMMIT 参与者 |
| 总供应上限 | – | 21,000,000 AII |
| 创世预分配 | – | 1,050,000 AII（5%，详见《06》§3） |

> 表中"原生代币"具体数额最终以《06 代币经济与分配模型》为准。

---

## 14. 兼容性与升级路径

- 任何破坏向后兼容的协议升级须通过 hard fork——节点运营者自愿升级到新版客户端，**无链上投票机制**。
- 客户端版本协商使用 `extraData` 字段携带版本号；不兼容客户端被自然孤立到分叉链。
- 紧急升级（如发现关键安全漏洞）走 SECRC 5/7 多签签发的紧急热补丁（详见《07 社区与协议演进》§4）。节点仍可拒绝运行任何版本。

---

## 15. 与以太坊（post-Merge）的偏差清单

下面列出**本链相对以太坊 Shanghai 主网的所有协议层偏差**：

**区块头**：

1. **删除** 字段：`difficulty`、`mixHash`、`nonce`、`ommersHash`、`uncleList`（无 PoW、无 uncle）
2. **新增** 字段：`slot`、`epoch`、`proposer`、`randaoMix`、`parentCommitSig`、`parentCommitBitmap`、`proposerSig`、`flushRoot`
3. `beneficiary` 字段移除（出块奖励直接发给 `proposer`，不再有单独的 coinbase 概念）

**共识**：

4. 共识由 PoW（Ethash）改为 **BFT-PoS（单区块即时最终性）**
5. 出块由 VRF 选举 + ⅔ stake 加权 PRE-COMMIT 决定
6. 无叔块（uncle）、无概率最终性、无 6 块确认
7. 罚没机制（slashing）：双签 100%、反向投票部分、长期离线累计

**协议扩展**：

8. 新增 Flush 类型交易与全局 `MotherChainRegistry` 合约（承载子链最终性）
9. 新增预编译合约 0x0a–0x0e
10. 新增子链状态根的并行维护（不影响主链状态根计算）
11. 新增 `mc_*` RPC 前缀
12. chainId 默认 99

除上述偏差外，**主链应用层一切语义（EVM、Gas、ABI、地址）与以太坊 Shanghai 完全相同**——已有 Solidity 合约可一键迁移。

---

## 本批次占位字段汇总

| 占位符 | 出现位置 | 建议候选 |
|---|---|---|
| `《[占位] 10》` | §0.2, §13 | 5 / 10 / 13 |
| `《[占位] 30》` | §0.2 Flush 周期 | 15 / 30 / 60 |
| `《[占位] 50000》` | §4.1 V 节点抵押 | 1万–10万 |
| `《[占位] 14》` | §4.1 冷却期 | 7 / 14 / 28 |
| `《[占位] 6》` | §0.2, §13 slot 时间 | 6 秒推荐 |
| `《[占位] 6》` | §4.4 主链最终性块数 | 6 / 12 |
| `《[占位] 3》` | §4.4 子链 epoch 数 | 3 / 5 |
| `《[占位] 30,000,000》` | §6.2 区块 gas 上限 | 与以太坊一致 |
| `《[占位] 99》` | §8 chainId | 待协调 |

— 本黄皮书完 —
