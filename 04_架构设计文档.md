# 04 AII 架构设计文档

> 版本：v0.1（初稿）
> 适用对象：核心开发团队、系统集成方、运维工程师
> 关联：本文档说明系统模块切分、接口定义、部署拓扑；协议级形式化定义见《03 黄皮书》；共识细节见《05 共识机制详细设计》。

---

## 0. 阅读路径

- 整体架构图与分层（§1, §2）
- 模块清单与职责（§3）
- 关键流程时序（§4）
- 接口规范（§5）
- 数据流与存储（§6）
- 部署拓扑（§7）
- 跨链子系统设计（§8）
- 性能与容量预估（§9）
- 可观测性与运维（§10）

---

## 1. 总体架构

本链采用**分层多链**架构，从上至下五层：

```
┌───────────────────────────────────────────────────────────────┐
│  Layer 5  生态层    DApp / 钱包 / 区块浏览器 / 行情 / DAO 工具   │
├───────────────────────────────────────────────────────────────┤
│  Layer 4  接入层    JSON-RPC / WebSocket / GraphQL / SDK       │
├───────────────────────────────────────────────────────────────┤
│  Layer 3  应用执行  子链（MicroChain）：EVM / WASM / 自定义 VM  │
│                    SCS 节点集群，可插拔共识                     │
├───────────────────────────────────────────────────────────────┤
│  Layer 2  共识协调  主链（MotherChain）：纯 PoS BFT（VRF + ⅔ PRE-COMMIT）│
│                    全局合约：VNodeStaking / MotherChainRegistry │
│                    / CrossChainRouter / GovernanceCore         │
├───────────────────────────────────────────────────────────────┤
│  Layer 1  P2P网络   devp2p / RLPx / libp2p（子链可选）          │
└───────────────────────────────────────────────────────────────┘
```

### 1.1 分层职责

| 层 | 职责 | 关键数据 | 关键参与者 |
|---|---|---|---|
| Layer 5 | 终端可视化与业务 | DApp 调用、用户余额查询 | 终端用户 |
| Layer 4 | 协议接入 | RPC 请求、事件订阅 | 钱包、SDK |
| Layer 3 | 业务执行 | 子链状态、合约存储 | SCS 节点、DApp 开发者 |
| Layer 2 | 安全与最终性 | 主链状态、Flush 记录、BFT 终结证书 | V 节点（提议者 + 见证者） |
| Layer 1 | 节点互联 | 节点目录、消息广播 | 全节点、引导节点 |

### 1.2 设计原则

1. **关注点分离**：主链只做"安全 + 治理"，子链只做"业务 + 性能"。
2. **最小可信内核**：主链协议保持简单稳定，复杂度上推到子链或合约层。
3. **向后兼容**：与以太坊存量生态（钱包、SDK、合约工具）保持二进制兼容。
4. **可治理**：所有关键参数都暴露为治理对象，无需硬分叉即可调整。
5. **故障隔离**：单条子链 bug / 性能问题不影响其他子链或主链。

---

## 2. 物理与逻辑视图

### 2.1 物理视图（节点分布）

```
              Internet
                 │
                  │
   ┌──────────────▼────────────────────────────┐
   │ V 节点池（PoS BFT — VRF 提议 + ⅔ PRE-COMMIT 终结）│
   └──────────────┬────────────────────────────┘
        │
        │ Flush + 路由
        │
   ┌────▼──────────────────────────────────┐
   │  SCS 节点集群                          │
   │  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
   │  │子链 #1   │ │子链 #2   │ │子链 #N   │   ...    (子链)
   │  │PoS      │ │PBFT     │ │DPoS     │  │
   │  └─────────┘ └─────────┘ └─────────┘  │
   └─────────────────────────────────────  ┘
                 ▲
                 │ RPC
                 │
        ┌────────┴────────┐
        │ 钱包 / DApp /    │
        │ 区块浏览器       │
        └─────────────────┘
```

### 2.2 逻辑视图（模块图）

详见 §3 模块清单。

---

## 3. 模块清单

下表列出客户端二进制 `aiid`（守护进程）中的核心模块。每个模块按 **Rust crate** 组织（位于单一 cargo workspace 下）。

| 模块 | 路径建议 | 职责 | 主要类型 |
|---|---|---|---|
| **core/block** | `core/block` | 区块/区块头/交易/收据数据结构 | `Block`, `Header`, `Tx`, `Receipt` |
| **core/state** | `core/state` | MPT、世界状态、账户读写 | `StateDB`, `Account`, `Trie` |
| **core/vm** | `core/vm` | EVM 解释器、Gas 计量、预编译 | `EVM`, `Interpreter`, `Precompile` |
| **core/wasm** | `core/wasm` | WASM 虚拟机（子链可选） | `WasmVM`, `Host` |
| **consensus/bft** | `consensus/bft` | 主链 BFT-PoS（VRF 提议者 + ⅔ stake PRE-COMMIT） | `BftEngine`, `Proposer`, `Voter` |
| **consensus/vnode** | `consensus/vnode` | V 节点签名验证、抵押管理 | `VNodeStaking`, `VSet` |
| **consensus/pos** | `consensus/pos` | 子链 PoS 实现 | `PoSEngine` |
| **consensus/pbft** | `consensus/pbft` | 子链 PBFT 实现 | `PBFTEngine` |
| **consensus/dpos** | `consensus/dpos` | 子链 DPoS 实现 | `DPoSEngine` |
| **consensus/iface** | `consensus/iface` | 可插拔共识接口 | `Engine` interface |
| **microchain** | `microchain` | 子链生命周期、Flush、调度 | `MicroChain`, `Scheduler`, `Flusher` |
| **crosschain** | `crosschain` | 跨链消息路由、HTLC、IBC 适配 | `Router`, `HTLC`, `IBCAdapter` |
| **net/p2p** | `net/p2p` | devp2p / RLPx | `Server`, `Peer`, `Discovery` |
| **net/libp2p** | `net/libp2p` | libp2p（子链可选） | `LibP2PHost` |
| **net/sync** | `net/sync` | 区块同步、Snap Sync | `BlockSync`, `SnapSync` |
| **net/txpool** | `net/txpool` | 交易池 | `TxPool` |
| **storage/leveldb** | `storage/leveldb` | LevelDB 封装 | `KVStore` |
| **storage/rocksdb** | `storage/rocksdb` | RocksDB 封装（备选） | – |
| **rpc** | `rpc` | JSON-RPC、WebSocket、GraphQL | `Server`, `Handler` |
| **cli** | `crates/aii-cli` | 用户面 CLI `aii`（基于 clap v4） | – |
| **mcp** | `crates/aii-mcp` | MCP Server（基于 rmcp，stdio/SSE/HTTPS）| `McpServer`, `Tool` |
| **wallet/keystore** | `wallet/keystore` | 本地密钥库 | `KeyStore` |
| **crypto** | `crypto` | secp256k1 / BLS / Keccak | – |
| **governance** | `governance` | 链上提案与投票 | `Proposal`, `Vote` |
| **metrics** | `metrics` | Prometheus 指标 | – |
| **cli** | `cmd/node` | 节点启动入口 | `main` |

### 3.1 模块间依赖

依赖方向自上而下，禁止反向依赖：

```
cli  →  rpc  →  microchain ┬→ consensus/iface → consensus/{pow,pos,pbft,dpos,vnode}
                          ├→ core/{state,vm,wasm}
                          ├→ crosschain
                          ├→ net/{p2p,sync,txpool}
                          └→ storage
                          
governance, wallet, crypto, metrics  作为基础模块被多方依赖
```

---

## 4. 关键流程时序

### 4.1 主链区块生产

```
slot s 开始（每 6 秒）:
   │
proposer P = VRF(epoch_seed, V_active_weighted, slot s)
   │
P:  [收集 mempool 交易 + Flush + 跨链证明，构造 B_s]
   │
P --(NewBlock B_s 广播)--> 全网 V_active
   │
V 节点们: [验证 B_s（状态转移 + Flush 证明）]
   │
V 节点 --(PRE-VOTE B_s)--> 全网
   │
[当任一节点收齐 ⅔ stake PRE-VOTE → 发 PRE-COMMIT]
   │
V 节点 --(PRE-COMMIT B_s)--> 全网
   │
[收齐 ⅔ stake PRE-COMMIT → B_s 即时 FINALIZED]
   │
下一 slot B_{s+1}.header.parentCommitSig 携带聚合 PRE-COMMIT 证书
```

### 4.2 子链合约调用与 Flush

```
用户:  [发送 sc 交易到子链 #1 RPC]
   │
SCS节点: [按子链共识达成出块]
   │
[子链每 K 秒出一个块；状态写入子链本地 stateDB]
   │
[每 T_flush 个主链区块时，V 节点中的子链协调者 Vc：]
   │
Vc:  [收集子链 #1 当前最新区块头和共识证明]
   │
Vc --(Flush 交易)--> 主链
   │
下一 slot proposer: [将 Flush 交易打包入下一主链区块]
   │
全网: [验证 Flush 证明，更新主链上子链状态根]
   │
最终性达成；SCS 节点奖励发放
```

### 4.3 跨链消息（子链 A → 子链 B）

```
合约A:  precompile.CrossChainSend(B, addr_B, payload)
   │
子链A SCS: 触发 CrossChainEvent，写入子链 receipts
   │
[等待下一次 Flush]
   │
子链A Flush 到主链：flushRoot 包含跨链事件根
   │
主链 CrossChainRouter：发现 targetChain=B，记录待投递队列
   │
子链B V 节点协调者 Vc'：监听主链事件，将跨链消息包装为子链B交易
   │
子链B SCS：执行 payload，调用 addr_B
   │
[等待子链B下一次 Flush，将处理结果反馈]
```

### 4.4 新子链上链流程

```
发起方:  调用 MotherChainRegistry.createMicroChain(params)
   │
主链合约: 验证抵押金、共识类型、参数；分配 microChainId（即合约地址）
   │
[发起方招募 SCS 节点：每个 SCS 节点调用 MotherChainRegistry.joinMicroChain(chainId, stake)]
   │
[V 节点选举一名子链协调者 Vc]
   │
Vc: 拉起子链共识，触发创世子链区块（含初始合约部署）
   │
子链上线，开始处理用户交易
```

### 4.5 子链 Gas 与计费

子链合约执行**不消耗主链 gas**。子链发起方在创建子链时定义计费模式：

| 模式 | 含义 | 适用 |
|---|---|---|
| `FREE` | 用户调用免费 | 公共服务子链（如 DAO 投票） |
| `LOCAL_GAS` | 用户用主链原生代币付费，但费率/上限独立 | 主流应用 |
| `CUSTOM_TOKEN` | 用户用子链定义的代币付费 | 联盟链 / 私链 |
| `SUBSIDIZED` | 子链发起方批量预付 | 企业 SaaS 模式 |

子链 gas 配置在 `MotherChainRegistry` 中可治理变更。

---

## 5. 接口规范

### 5.1 共识引擎接口（`consensus/iface`）

```go
type Engine interface {
    // 该引擎使用的算法类型 ID（见黄皮书 §4.3）
    Type() uint8
    
    // 验证区块头是否符合本共识
    VerifyHeader(chain ChainReader, header *Header) error
    
    // 准备出块（填写难度、签名占位等）
    Prepare(chain ChainReader, header *Header) error
    
    // 终结区块（区块奖励等）
    Finalize(chain ChainReader, header *Header, state *StateDB, txs []*Tx, receipts []*Receipt)
    
    // 给出出块所需的密封过程（PoS 收 PRE-COMMIT、PBFT 收 commit 等；本链已无 PoW）
    Seal(chain ChainReader, block *Block, results chan<- *Block, stop <-chan struct{}) error
    
    // 计算下一区块的共识参数（PoS 用：slot 时间、proposer 提示等；PoW 已不使用）
    CalcDifficulty(chain ChainReader, time uint64, parent *Header) *big.Int
    
    // 节点变更回调（V 节点加入/退出等）
    UpdateValidators(chain ChainReader, header *Header) error
}
```

每种共识算法（PoS BFT / PBFT / DPoS / Custom）实现该接口，注册到全局工厂（注：本链主链已无 PoW，但接口保留以备子链需要时使用）：

```go
RegisterEngine(typeID uint8, factory func(config []byte) Engine)
```

### 5.2 跨链路由接口

```go
type CrossChainRouter interface {
    // 子链 A 发送跨链消息
    Send(srcChain, dstChain ChainID, dstContract Address, payload []byte, value *big.Int) (msgID Hash, err error)
    
    // V 节点提交跨链证明
    Submit(proof CrossChainProof) error
    
    // 查询跨链消息状态
    Status(msgID Hash) (CrossChainStatus, error)
    
    // 订阅跨链事件
    Subscribe(filter CrossChainFilter) (Subscription, error)
}
```

### 5.3 子链管理接口

```go
type MicroChainManager interface {
    Create(params CreateParams) (ChainID, error)
    Destroy(chainID ChainID) error
    Pause(chainID ChainID) error
    Resume(chainID ChainID) error
    GetSCSSet(chainID ChainID, epoch uint64) ([]Address, error)
    GetStateRoot(chainID ChainID, blockNum uint64) (Hash, error)
    GetFlushHistory(chainID ChainID) ([]FlushRecord, error)
}
```

### 5.4 外部 RPC（节选）

参考《03 黄皮书》§11；完整接口在《11 开发者文档与 SDK 规划》中给出。

---

## 6. 数据流与存储

### 6.1 持久化数据分类

| 数据类型 | 引擎 | 大小估算（主网运行 1 年） | 备注 |
|---|---|---|---|
| 主链区块体 | LevelDB | ~50 GB | 每 10 秒 1 块，~2MB/块 |
| 主链区块头 | LevelDB | ~3 GB | – |
| 主链状态 MPT | LevelDB | ~80 GB → 200 GB+ | 随地址数增长 |
| 主链收据 | LevelDB | ~30 GB | – |
| 子链区块（单条） | LevelDB | 10–200 GB | 与子链 TPS 强相关 |
| 子链状态 | LevelDB | 5–100 GB | – |
| 索引（地址-tx, 区块号-哈希） | LevelDB | ~10 GB | 可重建 |

### 6.2 数据库选型

- **默认**：LevelDB（与以太坊一致，便于二进制兼容）
- **可选**：RocksDB（写性能更好，适合高 TPS 子链）
- **未来**：Badger / Pebble（纯 Go 实现）

详见《08 技术选型决策书》§4。

### 6.3 同步模式

支持四种同步：

| 模式 | 说明 | 适用 |
|---|---|---|
| **Full Sync** | 从创世区块逐块同步并执行 | 节点初始化 |
| **Fast Sync** | 同步区块头 + 状态快照 | 加快启动 |
| **Snap Sync** | 同以太坊 Snap Sync | 默认推荐 |
| **Light Sync** | 只同步区块头，状态按需查询 | 移动端 |

### 6.4 子链快照与归档

子链状态可由 SCS 节点定期拍快照（默认每 1000 个子链块），快照哈希随 Flush 上主链。

子链历史数据可选择性归档至 IPFS（详见 §8.2），SCS 节点只保留近 30 天热数据。

---

## 7. 部署拓扑

### 7.1 节点分级部署（7 Tier 模型）

AII 客户端**单一二进制 `aiid`** 可启动为 7 种 Tier 之一，由首次启动的自动识别推荐（详见 §14）。Tier 完整定义见《05 共识机制详细设计》§9.5，下面给出运维视角的部署要点：

| Tier | 角色 | 推荐硬件 | 命令示例 | 早期目标数量 |
|---|---|---|---|---|
| T1 | V 节点 | 16+ 核 / 64+ GB / 2+ TB NVMe / 1 Gbps | `aiid --role vnode --vnode-key <bls> --stake-from <addr>` | 21–101 |
| T2 | SCS 节点 | 8 核 / 32 GB / 1 TB NVMe / 100 Mbps | `aiid --role scs --microchain <id> --consensus pbft` | 子链 N × 21 |
| T3 | 归档全节点 | 8 核 / 32 GB / 5+ TB SSD / 100 Mbps | `aiid --role archive --rpc --rpc-addr 0.0.0.0:8545` | ≥ 10（社区运维） |
| T4 | 全节点 | 4 核 / 16 GB / 1 TB SSD / 100 Mbps | `aiid --role full --rpc --rpc-addr 0.0.0.0:8545` | 不限 |
| T5 | 修剪节点 | 2 核 / 8 GB / 500 GB / 50 Mbps | `aiid --role pruned --prune-window 128` | 不限 |
| T6 | 轻节点 | 1 核 / 2 GB / 50 GB / 5 Mbps | `aiid --role light` 或集成至桌面钱包 | 不限 |
| T7 | 移动钱包 | iOS / Android 设备 | 通过移动客户端集成，调用远程 RPC | 不限 |

#### 7.1.1 主链共识参与（无 PoW 矿工）

AII 主链共识为纯 PoS BFT，**不存在 PoW 矿工**：

- 出块由 VRF 选举的 V 节点（T1）轮值，无算力竞争
- 任何持有 ≥ S_min(V) 抵押的 T1 节点即可参与出块
- 早期目标 ≥ 21 个 V 节点跨地理分布，长期 ≥ 101

#### 7.1.2 引导节点（Bootnode）

- 由 AII DAO 社区运营至少 5 个跨地理区域的引导节点
- `aiid --role bootnode --listen-addr 0.0.0.0:30303`
- 引导节点列表硬编码于客户端，并通过 DNS（`bootnodes.aii.network`）动态发现新加入的引导节点

### 7.2 地理分布建议

- 北美：≥ 25% 节点
- 欧洲：≥ 20% 节点
- 东亚：≥ 30% 节点
- 其他：剩余
- 单云厂商占比 ≤ 30%

### 7.3 防火墙端口

| 端口 | 协议 | 用途 |
|---|---|---|
| 30303 | TCP/UDP | devp2p |
| 30304 | TCP | libp2p（子链可选） |
| 8545 | TCP | HTTP RPC |
| 8546 | TCP | WebSocket RPC |
| 8547 | TCP | GraphQL |
| 9090 | TCP | Prometheus 指标 |
| 6060 | TCP | pprof（仅内网） |

---

## 8. 跨链子系统设计

### 8.1 内部跨链：主链作为消息总线

详见《03 黄皮书》§9.1。要点：

- **不引入额外信任**：跨链消息以主链共识为信任根。
- **异步交付**：消息确认延迟 ≤ 2 × Flush 周期（默认 ≤ 10 分钟），即时性应用建议保留在同一子链内。
- **失败回退**：目标子链拒绝消息（如 gas 不足、合约 revert），消息回退至源子链，触发回调。

### 8.2 外部跨链：双桥架构

外部跨链既支持**轻量**（HTLC 原子交换）也支持**通用**（双签桥）：

#### 8.2.1 HTLC 原子交换桥

- 适用：BTC ↔ 本链；少量、用户主导。
- 实现：本链上部署 `HTLCBridge` 合约，BTC 侧由用户脚本完成。
- 安全：仅依赖哈希函数与超时假设；**无第三方信任**。

#### 8.2.2 多签验证者桥

- 适用：ETH ↔ 本链、企业接入。
- 实现：源链锁仓 → 验证者多签 → 目标链铸造镜像代币。
- 验证者：从 V 节点中选举出的桥验证者集（默认 15/21 阈值签名）。
- 安全：依赖验证者诚实假设；同时引入抵押 + slashing 经济约束。

#### 8.2.3 IBC 适配器

- 适用：Cosmos 生态接入。
- 实现：本链实现 ICS-002 / ICS-003 / ICS-004 / ICS-020 接口，作为 IBC 节点接入 Cosmos Hub。
- 安全：依赖 IBC 协议本身的轻客户端验证机制。

#### 8.2.4 XCM 适配器

- 适用：Polkadot 生态接入（路线图 Phase 5+）。
- 实现：通过 Snowbridge 类方案或自研中继。

### 8.3 跨链监控

AII DAO运营独立的 **CrossChainObserver** 服务：

- 实时监控所有跨链桥的余额一致性
- 异常资金流出立即告警
- 监控数据公开 dashboard

---

## 9. 性能与容量预估

### 9.1 主链

| 指标 | 目标 | 备注 |
|---|---|---|
| 出块时间 | 10 秒 | – |
| 区块 gas 上限 | 30,000,000 | 与以太坊一致 |
| 主链 TPS（基础转账） | ~1,500 | gas 上限 / 21000 / 10s |
| 主链 TPS（智能合约） | 50–200 | 视合约复杂度 |
| 状态增长（GB/年） | 80–200 | 取决于 DApp 活跃度 |

### 9.2 子链

| 指标 | 目标 | 备注 |
|---|---|---|
| 子链数量上限 | 无硬上限，软上限由 V 节点容量决定 | 早期 50 条，远期 5000+ |
| 单条子链 TPS（PBFT） | 3,000–5,000 | 21 SCS 节点 |
| 单条子链 TPS（DPoS） | 5,000–10,000 | 21 见证人 |
| 单条子链 TPS（PoS） | 1,000–3,000 | – |
| 全网聚合 TPS | 150,000+ | 由活跃子链数决定 |

### 9.3 跨链

| 指标 | 目标 |
|---|---|
| 内部跨链延迟 | ≤ 10 分钟（2 × Flush 周期） |
| 外部跨链（HTLC）延迟 | 1–4 小时（BTC 6 确认） |
| 外部跨链（多签桥）延迟 | 主链 6 确认 + 桥验证 ≈ 5 分钟 |

---

## 10. 可观测性与运维

### 10.1 关键指标（Prometheus）

| 指标族 | 关键 metric | 告警阈值（建议） |
|---|---|---|
| 出块 | `block_production_lag_seconds` | > 30s |
| 共识 | `vnode_signature_missing_rate` | > 5% |
| 同步 | `peer_count`, `sync_lag_blocks` | peer<3 / lag>100 |
| 内存 | `txpool_size`, `state_cache_size` | – |
| 子链 | `flush_delay_seconds[microchain_id]` | > 600s |
| 跨链 | `cross_chain_pending_count` | > 100 |
| Gas | `gas_used_ratio` | > 0.95 持续 |

### 10.2 日志与追踪

- 结构化日志：JSON，按等级 `debug/info/warn/error/fatal`
- 关键事件强制 audit log（区块产出、共识投票、Flush、跨链）
- 分布式追踪：OpenTelemetry，子链协调者作为 trace root

### 10.3 升级与热补丁

- 节点支持滚动升级，社区客户端版本需保持向后兼容 ≥ 2 个 minor 版本
- 紧急补丁通过AII DAO签名公告 + 节点自动验证签名拉取

---

## 11. 安全分区

- **主链节点**：必须开启所有安全机制（防火墙、TLS、密钥管理）。
- **V 节点**：要求 HSM（硬件安全模块）保护抵押私钥。
- **桥验证者**：HSM + 离线签名 + 多人监管。
- **RPC 节点**：默认禁用危险接口（`personal_*`, `admin_*`, `debug_*`），仅在内网开启。

详细威胁模型见《09 安全与威胁模型》。

---

## 12. AII 自主架构摘要

| 维度 | 本链设计 | 工程影响 |
|---|---|---|
| 主链客户端语言 | **Rust 全栈** | 从零构建 Rust 实现 |
| 子链 VM | EVM + WASM | VM 接口需抽象 |
| 共识引擎可插拔 | 是（接口扩展） | 接口签名需保持稳定 |
| 跨链 | 原子交换 + IBC/XCM | 新增适配器模块 |
| 节点密钥保护 | 文件密钥库 + HSM 支持 | 新增 hsm 模块 |
| 同步模式 | Full/Fast + Snap/Light | 新增同步策略 |

---

## 13. 决策待回答

| # | 问题 | 影响模块 | 建议默认值 |
|---|---|---|---|
| 1 | 主链语言 | 全部 | **Rust 全栈**（已决定）|
| 2 | 是否支持账户抽象（EIP-4337）？ | core/vm, txpool | 路线图 Phase 4 引入 |
| 3 | 是否引入 EIP-1559 费用市场？ | core/block, txpool | 主网上线即引入 |
| 4 | 是否启用 zk-rollup 子链类型？ | consensus, vm | Phase 5+ 引入 |
| 5 | 默认 P2P 是 devp2p 还是 libp2p？ | net/p2p | devp2p（兼容以太坊） |
| 6 | 状态树用 MPT 还是 Verkle？ | core/state | MPT 起步，Phase 5+ 迁 Verkle |

---

## 13.5 AI 集成层（MCP + CLI）

AII 协议级提供两条 AI 友好接入轨，详细设计见《12 AI 集成 — MCP 与 CLI》：

- **CLI `aii`**：覆盖 wallet / tx / contract / block / state / subchain / crosschain / dao / node 全部领域；机器可读输出（JSON / Markdown / YAML）；`--llm` 模式自动产出 prompt 友好格式。
- **MCP Server**：`aii ai serve` 启动 Model Context Protocol 服务，暴露 22 个只读工具 + 10 个写入工具；私钥本地隔离，写操作走"确认即签"流程；支持 stdio / SSE / HTTPS+token 三种传输。

CLI 与 MCP 共享 `aii-core` 内核，**无能力差异**，仅接入面不同。

---

## 14. 客户端架构（多端覆盖）

AII 客户端采用 **"统一 Rust 内核 + 平台外壳"** 架构：

```
┌─────────────────────────────────────────────────────────────┐
│   PC 端                                          移动端         │
│  ┌───────────────────┐                  ┌─────────────────┐ │
│  │ Windows / macOS / │                  │ iOS / Android   │ │
│  │ Linux 桌面应用    │                  │ 原生 App        │ │
│  │  (Tauri + Web UI) │                  │ (Swift/Kotlin   │ │
│  │                   │                  │  + Rust FFI)    │ │
│  └─────────┬─────────┘                  └────────┬────────┘ │
│            │                                     │          │
│            └────────────┬────────────────────────┘          │
│                         │                                   │
│                  ┌──────▼──────┐                            │
│                  │  Rust Core  │                            │
│                  │  (aii-core) │  - 节点核心 (T1-T7 全 Tier) │
│                  │             │  - P2P / 共识 / 状态        │
│                  │             │  - 钱包 / 签名              │
│                  │             │  - RPC 客户端 / 服务端       │
│                  └──────┬──────┘                            │
│                         │                                   │
│        ┌────────────────┼───────────────────┐               │
│        ▼                ▼                   ▼               │
│   嵌入式数据库       libp2p P2P          密码学库            │
│   (RocksDB/redb)     (rust-libp2p)      (RustCrypto)        │
└─────────────────────────────────────────────────────────────┘
```

### 14.1 PC 桌面端

| 平台 | 包格式 | 框架 | 备注 |
|---|---|---|---|
| Windows | `.msi` / `.exe` 安装包 + 便携 `.zip` | Tauri 2 + Vue 3 / React | 适配 x64 / ARM64 |
| macOS | `.dmg` 签名包 + Homebrew cask | Tauri 2 + Vue 3 / React | Universal Binary（Intel + Apple Silicon） |
| Linux | `.deb` / `.rpm` / AppImage / Flatpak / Snap | Tauri 2 + Vue 3 / React | x64 / ARM64 |

**为什么 Tauri 而非 Electron**：
- Rust 后端可直接嵌入完整节点内核（Electron 需 Node.js 桥）
- 安装包体积小 10×（Tauri ~10MB / Electron ~150MB）
- 内存占用低 3–5×
- 与本链 Rust 栈一致，代码可复用

PC 桌面端**默认支持运行 T3–T6 任一 Tier**。T1/T2 节点也可在桌面端跑，但更多人会用纯服务器命令行 `aiid` 部署。

### 14.2 移动端

| 平台 | 实现 | 备注 |
|---|---|---|
| iOS | Swift / SwiftUI + Rust（通过 UniFFI）| 支持 iPhone / iPad |
| Android | Kotlin / Jetpack Compose + Rust（通过 JNI / cargo-ndk） | API 26+（Android 8+）|

**共享 Rust 核心**：

- 钱包密钥派生（BIP-32/39/44）、交易签名、地址生成
- RPC 客户端（与远程 T3/T4 节点对话）
- 轻节点（T6）区块头同步
- 跨链消息构造
- 与桌面端**复用同一份 Rust 代码**，仅替换 UI 层

**移动端限制**：

- 仅承担 T6 / T7 角色（不参与共识，不存储完整链）
- 默认走远程 RPC（社区运营节点 + 用户可自定义）
- iOS 受 Apple 沙箱限制不能开放监听端口；Android 在前台服务下可开 libp2p
- 节点完整数据**不存设备**，避免占用过多空间

### 14.3 浏览器扩展钱包

| 平台 | 实现 | 备注 |
|---|---|---|
| Chrome / Edge / Brave | TypeScript + WASM（编译自 aii-core 钱包模块） | 与 MetaMask 兼容协议 |
| Firefox | 同上 | – |
| Safari | Safari Web Extension | 受沙箱限制更多 |

Web Extension 通过 WASM 共享 Rust 钱包/签名逻辑，避免 JS 重复实现。

### 14.4 客户端自动识别模块（First-Run Wizard）

所有平台的客户端在首次启动时运行**统一的 Rust 模块** `aii_core::onboarding`：

```rust
// aii-core/src/onboarding.rs
pub struct HardwareProfile {
    pub cpu_cores: usize,
    pub ram_gb: u64,
    pub disk_free_gb: u64,
    pub disk_type: DiskType,    // HDD / SATA SSD / NVMe
    pub disk_iops_4k: u32,      // measured
    pub public_ip: bool,
    pub upstream_mbps: u32,     // measured
    pub bootstrap_latency_ms: u32,
}

pub fn detect() -> HardwareProfile { ... }

pub fn score(profile: &HardwareProfile) -> u32 { ... }

pub fn recommend_tier(score: u32) -> Tier {
    match score {
        85.. => Tier::T1_Validator,
        65..=84 => Tier::T2_Scs,
        50..=64 => Tier::T3_Archive,
        35..=49 => Tier::T4_Full,
        20..=34 => Tier::T5_Pruned,
        10..=19 => Tier::T6_Light,
        _ => Tier::T7_Mobile,
    }
}
```

**UI 层**（PC Tauri / 移动 Native）只负责调用此模块、展示结果、让用户确认或覆盖。Tier 评分与推荐逻辑详见《05 共识机制详细设计》§9.5。

### 14.5 客户端升级机制

| 平台 | 升级方式 |
|---|---|
| Windows / macOS / Linux | 内置 Sparkle/Tauri Updater，签名验证后增量升级 |
| iOS / Android | 应用商店推送 |
| 服务器命令行 | `aiid self-update` 或 Docker image tag 更新 |
| Web 扩展 | 浏览器商店推送 |

所有升级包必须由 SECRC 多签签名（详见《07 社区与协议演进》§6），客户端验证签名后才接受升级。

### 14.6 多端同账户体验

用户在 PC 钱包导入助记词后，可通过**链下加密同步**（如 iCloud Keychain / Google Drive 加密 blob）或扫码同步至移动端。无任何中心化服务器参与——同步包由用户私钥加密。

---

## 本批次占位字段汇总

| 占位符 | 出现位置 | 建议候选 |
|---|---|---|
| `aiid` | §3, §7, §14 | 已定为 `aiid`（守护进程统一名） |
| `《[占位] 50000》` | §7.1 | 与黄皮书一致 |

— 本架构设计文档完 —
