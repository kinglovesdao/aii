# 06 AII技术选型决策书

> 版本：v0.1（初稿）
> 适用对象：CTO、技术决策委员会、首席架构师
> 关联：本文档列出主要技术栈的多个备选方案、各自优劣，并给出**推荐选项**与理由。最终决策应在主网启动前由技术委员会确认并写入《03 架构设计文档》。

---

## 0. 决策框架

每项技术选型按以下五维评分（1–5 分）：

| 维度 | 含义 |
|---|---|
| **生态成熟度** | 既有库、工具、文档、社区支持 |
| **性能** | 吞吐、延迟、资源占用 |
| **安全** | 漏洞历史、密码学审计、形式化能力 |
| **团队熟悉度** | 学习曲线、招聘可得性 |
| **兼容性** | 与以太坊 / MOAC 现有生态的可对接性 |

---

## 1. 主链客户端开发语言

### 1.1 备选与评分

| 语言 | 生态 | 性能 | 安全 | 内存安全 | 兼容性 | 合计 |
|---|---|---|---|---|---|---|
| **Rust** | 4 | 5 | 5 | 5 | 4 | **23** |
| Golang | 5 | 4 | 4 | 3 | 5 | 21 |
| C++ | 5 | 5 | 3 | 2 | 4 | 19 |
| Zig | 2 | 5 | 4 | 4 | 3 | 18 |

### 1.2 决策：**Rust 为主**

- **主链客户端、节点守护进程、RPC、P2P、共识引擎、VM、状态树、密码学** → **Rust**
- **理由**：
  - 内存安全：Rust 编译期保证无空指针、无数据竞争，对**链节点这种长期运行、对漏洞零容忍**的系统是首选
  - 性能：与 C/C++ 同级，无 GC 暂停（Go 在重负载下 GC 停顿对共识不利）
  - 现代生态：Solana、Polkadot、Aptos、Sui、Near、Filecoin、Aleo、Mina、CKB、Linea 等新一代公链均用 Rust 实现核心节点
  - WASM 原生：Rust 是 WASM 生态一等公民，便于实现子链 WASM VM
  - 关键密码学库（BLS、Verkle、zkSNARK）的最优实现都在 Rust
  - 与 Substrate / reth / sp1 / Geth-rust 等成熟代码可借鉴

> **差异化决策**：相对 MOAC 原方案使用 Golang，本链改用 Rust 作为主链客户端语言。理由：MOAC 立项于 2017 年，当时 Rust 区块链生态尚不成熟；2024 年后 Rust 在公链领域已成主流，性能与安全优势明显，且参考实现充足。

### 1.3 其他模块语言选择

| 模块 | 语言 | 理由 |
|---|---|---|
| 主链节点 | **Rust** | 见 §1.2 |
| 子链共识引擎库 | **Rust** | 与主链共享内核 |
| 密码学库 | **Rust** | 最佳生态 |
| WASM 子链 VM | **Rust**（基于 Wasmtime / Wasmer） | – |
| EVM 实现 | **Rust**（基于 revm） | 比 go-ethereum core/vm 性能高 30%+ |
| 区块浏览器后端 | Rust（基于 Blockscout 重写）或 TypeScript | 视团队偏好 |
| 钱包桌面端 | **Rust**（基于 Tauri 框架）或 Electron | Tauri 体积小 10×、内存占用低 |
| 钱包移动端 | Swift（iOS）/ Kotlin（Android）或 Rust + Flutter | – |
| 钱包 Web 端 | TypeScript + React/Vue | 生态决定 |
| 浏览器扩展钱包 | TypeScript（必须） | 浏览器约束 |
| 节点运维脚本 | Bash / Python | – |
| 文档站点 | Markdown + VitePress / mdBook（Rust） | – |
| CI / 构建工具 | Cargo + GitHub Actions | – |

### 1.4 团队组建说明（开源项目）

由于 AII 是纯开源项目（详见《07 治理与社区架构》），不存在"招聘"，但社区在引导贡献者时应优先吸引：

1. 有 Rust 区块链经验者（前 Substrate / Solana / reth / Polkadot 贡献者）
2. 有以太坊协议层经验者（geth / erigon / lighthouse / prysm 贡献者）
3. 有密码学背景者（BLS / VRF / zk 研究者）

通过 DAO Grants 资助这些个人，而非通过雇佣合同。

---

## 2. P2P 网络栈

### 2.1 备选

| 方案 | 说明 | 优 | 劣 |
|---|---|---|---|
| **devp2p / RLPx** | 以太坊 P2P 栈 | 与以太坊完全兼容，钱包/浏览器友好 | 协议陈旧，扩展性弱 |
| **libp2p** | Protocol Labs 通用 P2P 栈 | 模块化、多传输、IPFS 同款 | 与以太坊不兼容 |
| **QUIC + 自研** | 基于 QUIC 协议 | 低延迟、内建加密 | 实现成本高 |

### 2.2 推荐：主链 devp2p，子链可选 libp2p

- **主链层**：sticky to **devp2p**，保证以太坊钱包/浏览器/工具直接可用。
- **子链层**：默认 devp2p；若子链发起方需要 libp2p（如对接 IPFS 或 Filecoin），开放选项。

### 2.3 协议升级路径

- **Phase 2**：devp2p 全栈
- **Phase 4**：评估 libp2p 引入子链
- **Phase 5+**：评估 devp2p over QUIC

---

## 3. 虚拟机（VM）

### 3.1 备选

| VM | 类型 | 优 | 劣 |
|---|---|---|---|
| **EVM** | 256-bit 栈机 | 生态最大、Solidity/Vyper 工具链成熟 | 性能受限、Gas 模型粗粒 |
| **eWASM** | WASM 子集 | 接近原生性能、多语言 | 标准未完全落地 |
| **WASM**（通用） | WebAssembly | 多语言（Rust/Go/C++/AssemblyScript） | 生态分散 |
| **Move VM** | Aptos/Sui 风格 | 资源类型安全、形式化能力 | 工具链尚新 |
| **CKB-VM**（RISC-V） | RISC-V | 灵活、未来感强 | 学习成本极高 |

### 3.2 推荐：主链 EVM，子链 EVM + WASM 双轨

- **主链 VM**：**EVM**（与以太坊完全兼容，主网启动即支持所有以太坊合约）。
- **子链可选 VM**：
  - `vm=evm` 默认
  - `vm=wasm` 可选（基于 Wasmtime，支持 Rust/AssemblyScript 合约）
- **未来路线**：Phase 5+ 评估 Move VM 子链；Phase 6+ 评估 ZK-VM 子链。

### 3.3 EVM 实现选型

| 实现 | 语言 | 备注 |
|---|---|---|
| **revm** | Rust | **主推**：与主链客户端语言一致，性能最佳，bluealloy/revm 已被 reth、Foundry 等项目广泛验证 |
| evmone | C++ | 备选，通过 FFI 嵌入；性能略高于 revm 但生态较弱 |
| go-ethereum core/vm | Go | 仅用于参考实现对比 |
| sputnikvm | Rust | 旧版 Rust EVM，不再推荐 |

**默认决策**：主链与子链 EVM 一律基于 **revm**。

---

## 4. 存储引擎

### 4.1 备选

| 引擎 | 说明 | 读 | 写 | 空间 | 备注 |
|---|---|---|---|---|---|
| **LevelDB** | Google KV | ★★★★ | ★★★ | ★★★ | go-ethereum 默认 |
| **RocksDB** | Facebook fork | ★★★★ | ★★★★★ | ★★★ | 调参复杂 |
| **Pebble** | CockroachDB fork | ★★★★ | ★★★★ | ★★★ | 纯 Go，无 cgo |
| **Badger** | Dgraph KV | ★★★★ | ★★★★ | ★★ | 纯 Go |

### 4.2 推荐：主链 **RocksDB**（Rust binding），子链可选

- **主链节点**：**RocksDB**（通过 rust-rocksdb 绑定），写吞吐最强，压缩友好，与 reth 等 Rust 主流公链节点保持一致。
- **状态树**：基于 RocksDB 实现 MPT；后期评估 Verkle Tree。
- **高 TPS 子链**：可继续 RocksDB 或选 **Sled**（纯 Rust，无 FFI）。
- **未来**：评估 **redb**（纯 Rust 单文件嵌入式数据库，无外部依赖）作为轻量子链选项。

---

## 5. 密码学库

### 5.1 备选

| 库 | 语言 | 覆盖 | 备注 |
|---|---|---|---|
| **k256** | Rust | secp256k1（RustCrypto） | 纯 Rust |
| **tiny-keccak** | Rust | Keccak-256 | 高性能 |
| **arkworks-rs** | Rust | BLS, pairing, zkSNARK | **最强 ZK 库** |
| **blst** | Rust + 汇编 | BLS12-381 | Supranational 出品，性能最优 |
| **schnorrkel / curve25519-dalek** | Rust | Ed25519 / Sr25519 / VRF | – |
| libsodium | C | 通用 | 仅在特殊场景通过 FFI |

### 5.2 推荐（全 Rust）

| 用途 | 库 |
|---|---|
| secp256k1 / Keccak | RustCrypto: k256 + tiny-keccak |
| BLS12-381（V 节点签名、PBFT 聚合）| **blst** |
| VRF（V 节点选举） | schnorrkel |
| 未来 ZK 子链 | **arkworks-rs** + **plonky3** + **sp1** |
| 默克尔树/MPT | trie-db / patricia-trie |

所有密码学库须接受**独立安全审计**（候选：Trail of Bits、Cure53、Open Crypto Audit Project、Least Authority），主网启动前完成。

---

## 6. 共识引擎库

### 6.1 PoW（Ethash）

参考 **reth** / **erigon** 的 Rust Ethash 实现；主网前评估是否切换为 ProgPoW 或 KawPow（更抗 ASIC）。

### 6.2 PoS

参考 **Lighthouse**（Rust，以太坊共识客户端）的 PoS 实现；自研轻量化 PoS 引擎用 Rust。

### 6.3 PBFT

参考 **Tendermint** 算法思想，用 Rust 重新实现 PBFT（不复用 Cosmos tendermint-go 二进制依赖）。子链 PBFT 共识由 V 节点协调，调度模型与 Tendermint 不同。

### 6.4 DPoS

参考 **EOS** / **TRON** 见证人模型，用 Rust 简化实现。

---

## 7. 跨链栈

### 7.1 HTLC 原子交换

- 自研合约 + 钱包侧 SDK（参考 Lightning Network 的 HTLC 实现）
- 与 BTC 交互：使用 **rust-bitcoin**（最成熟的 Rust BTC 库）作为协议解析与签名层

### 7.2 IBC

- 复用 **ibc-rs**（Cosmos / Informal Systems 的 Rust IBC 实现），改造其依赖以接入本链状态。
- 工作量评估：3 人月

### 7.3 XCM

- 路线图 Phase 5+ 评估
- 候选方案：Snowbridge 风格中继 / 直接实现 XCM v3 接口

### 7.4 多签桥

- 智能合约部分：Solidity 编写，模仿 **Wormhole** / **Multichain** 的成熟模式（吸取被攻击教训）。
- 验证者集合：从 V 节点中选举出 21 名桥验证者，15/21 阈值签名。

---

## 8. IPFS 与去中心化存储

### 8.1 备选

| 方案 | 说明 |
|---|---|
| **IPFS（Kubo）** | Protocol Labs 官方实现 |
| **Filecoin** | 含激励的 IPFS |
| **Arweave** | 永久存储 |
| **Swarm** | 以太坊家 |

### 8.2 推荐：IPFS Pinning 子链 + Filecoin 集成

- 本链上层提供 **IPFS Pinning 子链**：SCS 节点同时充当 IPFS Pinning Node，为 DApp 提供存储服务。
- 与 Filecoin 跨链：长期归档场景的数据自动迁移到 Filecoin。

---

## 9. 客户端与浏览器

### 9.1 客户端 / 钱包多端策略

AII 采用 **"统一 Rust 内核（aii-core）+ 平台外壳"** 架构（架构图详见《03 架构设计文档》§14）。客户端 = 节点 + 钱包二合一，平台覆盖：

| 平台 | 包格式 | 外壳框架 | 内核接入方式 | 最高支持 Tier |
|---|---|---|---|---|
| **Windows** (x64 / ARM64) | `.msi` / `.exe` / portable `.zip` | **Tauri 2** + Vue/React | 内嵌 aii-core 静态库 | T1–T6 |
| **macOS** (Intel + Apple Silicon) | `.dmg` 签名 / Homebrew | **Tauri 2** + Vue/React | 内嵌 aii-core | T1–T6 |
| **Linux** (x64 / ARM64) | `.deb` / `.rpm` / AppImage / Flatpak / Snap | **Tauri 2** + Vue/React | 内嵌 aii-core | T1–T6 |
| **iOS** (iPhone / iPad) | App Store IPA | **Swift / SwiftUI** | **UniFFI** 绑定 aii-core | T6 / T7 |
| **Android** (8.0+) | Google Play APK / Aurora Store | **Kotlin / Jetpack Compose** | **cargo-ndk + JNI** 绑定 aii-core | T6 / T7 |
| **Chrome / Edge / Brave / Firefox** | Web Extension | TypeScript + UI 框架 | **WASM**（aii-core 编译） | 仅签名 |
| **Safari** | Safari Web Extension | TS + Native Swift 桥 | WASM | 仅签名 |
| **服务器 / 命令行** | 单二进制 `aiid` + Docker / K8s Helm | 无 UI | 直接 aii-core | T1–T7 |
| **硬件钱包** | 适配 Ledger / Trezor / OneKey | 厂商固件 | 提交 App | 仅签名 |

### 9.2 框架选型理由

| 选择 | 拒绝的备选 | 理由 |
|---|---|---|
| Tauri 2 | Electron | Rust 后端可直接嵌完整节点内核；包体积小 10×；内存低 3–5× |
| Swift 原生 | React Native / Flutter | 移动端需直接调用密码学/P2P，原生 + UniFFI 性能与稳定性更优 |
| Kotlin 原生 | React Native / Flutter | 同上 |
| UniFFI | C-header 手写 FFI | Mozilla 出品，自动生成多语言绑定，Rust 生态共识 |
| cargo-ndk + JNI | UniFFI Kotlin | UniFFI Android 绑定生成稍弱，cargo-ndk + JNI 控制力更强 |
| WASM（浏览器） | 纯 JS 实现 | 与 Rust 内核同源代码，避免重复实现密码学 |
| Tauri Updater / Sparkle | 自研更新通道 | 成熟、签名验证开箱即用 |

### 9.3 单一代码库（Monorepo 推荐布局）

```
aii/
├── crates/
│   ├── aii-core/         # 节点核心：共识、状态、P2P、VM
│   ├── aii-wallet/       # 钱包：密钥、签名、地址
│   ├── aii-rpc/          # JSON-RPC 服务端/客户端
│   ├── aii-onboarding/   # 硬件识别 + Tier 推荐
│   └── aii-bindings/     # UniFFI / WASM / JNI 跨平台绑定生成
├── apps/
│   ├── aiid/             # 命令行守护进程
│   ├── desktop/          # Tauri 桌面应用（共用 web-ui）
│   ├── ios/              # Xcode 工程
│   ├── android/          # Gradle 工程
│   ├── webext/           # 浏览器扩展
│   └── web-ui/           # 共享前端（Vue/React 单页）
├── docs/                 # 用户文档 + 开发者文档
└── Cargo.toml            # workspace
```

### 9.4 硬件钱包适配

| 厂商 | 适配方式 |
|---|---|
| Ledger | 提交 Ledger Live App（Nano S Plus / Nano X / Stax） |
| Trezor | 通过 Trezor Suite 与 trezord 桥 |
| OneKey | 类 Ledger，通过其官方 SDK |
| Keystone | QR-Code 通信，无需 USB |

### 9.2 区块浏览器

| 选项 | 说明 |
|---|---|
| **Blockscout** | 开源，与以太坊兼容，主推 |
| Etherscan-style 自研 | 成本极高，不推荐 |

主推：Blockscout 二次开发，集成子链多链视图。

---

## 10. 测试与基础设施

### 10.1 单元 / 集成测试

| 框架 | 用途 |
|---|---|
| **cargo test** | Rust 单元测试 |
| **proptest** | 基于属性的随机测试 |
| **criterion** | 基准测试与回归检测 |
| **testcontainers-rs** | 集成测试启容器 |
| **insta** | 快照测试（用于状态/序列化兼容性） |

### 10.2 性能与压力测试

- **K6** / **vegeta**：RPC 压测
- **txgen**（自研，Rust）：链上交易压力发生器
- **reth / erigon 对比测试套件**：性能对比基线

### 10.3 形式化验证

- **TLA+**：建模共识算法，主网启动前完成 V 节点 PoS 切换协议建模
- **Coq / Lean 4**：核心算法形式化（可选，Phase 5+）

### 10.4 CI/CD

- **GitHub Actions**：开源 CI（公共仓库）
- **GitLab CI**：内部仓库
- **Drone CI**：备选
- 构建产物：跨平台二进制（Linux amd64/arm64, macOS amd64/arm64, Windows amd64），自动化签名

### 10.5 部署与运维

| 组件 | 工具 |
|---|---|
| 节点容器化 | Docker + Kubernetes Helm Chart |
| 监控 | Prometheus + Grafana |
| 日志 | Loki / ELK |
| 追踪 | Jaeger / Tempo |
| 告警 | Alertmanager + PagerDuty / Opsgenie |
| 密钥管理 | HashiCorp Vault + HSM |

---

## 11. 第三方开源依赖审计政策

- 所有引入的开源库须有清晰的 License（MIT/Apache-2.0/BSD/LGPL，避免 GPL/AGPL）
- 主链关键路径上的依赖须有 ≥ 2 年活跃维护历史 + ≥ 1 万 stars 或同等社区
- 每次升级版本走代码审查 + 安全公告订阅（GitHub Security Advisories、RustSec、CVE）
- 定期（每季度）执行依赖漏洞扫描（`cargo audit` / `cargo deny` / `npm audit`）
- Rust 项目优先使用 `cargo-vet` 审计第三方 crate

---

## 12. 与 MOAC 技术栈对照

| 维度 | MOAC | AII 推荐 | 偏差度 |
|---|---|---|---|
| 主链语言 | **Go** | **Rust** | 完全替换 |
| EVM 实现 | go-ethereum core/vm | **revm**（Rust） | 替换 |
| 子链 VM | EVM | EVM + WASM 双轨 | 扩展 |
| P2P | devp2p | devp2p（rust 实现，子链可选 libp2p-rust） | 同协议、不同语言 |
| 共识 | PoW + 子链可插拔 | PoW + V 节点 + 子链可插拔 | 增强 |
| 存储 | LevelDB | **RocksDB**（rust binding，可选 Sled / redb） | 替换 |
| 密码学 | go-ethereum/crypto + kilic | **RustCrypto + blst + arkworks** | 全替换 |
| 跨链 | 原子交换 | 原子交换 + IBC（ibc-rs）+ 多签桥 + XCM | 大幅扩展 |
| 钱包 | 多端 | 多端（含 MetaMask 兼容 + Tauri 桌面） | 升级 |

---

## 13. 关键技术决策结论汇总

| # | 决策 | 推荐 |
|---|---|---|
| 1 | 主链客户端语言 | **Rust**（全栈） |
| 2 | 主链 P2P | devp2p / RLPx（Rust 实现） |
| 3 | 主链 EVM 实现 | **revm**（Rust） |
| 4 | 子链 VM | EVM（revm）默认 + WASM（wasmtime）可选 |
| 5 | 主链存储 | **RocksDB**（rust-rocksdb） |
| 6 | 主链 PoW 算法 | Ethash（主网启动）→ ProgPoW 评估 |
| 7 | V 节点签名算法 | BLS12-381（**blst**） |
| 8 | 主链最终性 | 6 块概率最终性 + Flush 强最终性 |
| 9 | 跨链栈 | HTLC（rust-bitcoin）+ 多签桥 + IBC（ibc-rs）+（Phase 5+）XCM |
| 10 | IPFS 存储 | Pinning 子链 + Filecoin 后期归档 |
| 11 | 钱包多端 | MetaMask 兼容（Web）+ Tauri（桌面）+ Native（移动） |
| 12 | 区块浏览器 | Blockscout 二开 或自研 Rust 后端 |
| 13 | 监控栈 | Prometheus + Grafana + Loki |
| 14 | 密码学库 | RustCrypto + blst + arkworks-rs |
| 15 | 测试栈 | cargo test + proptest + criterion + insta |

---

## 14. 决策待回填

| # | 决策项 | 候选 | 截止时间 |
|---|---|---|---|
| 1 | PoW 算法（Ethash vs ProgPoW vs KawPow） | – | T0 - 6 月 |
| 2 | 子链 WASM 运行时（Wasmtime vs Wasmer） | – | T0 - 6 月 |
| 3 | 默认 IPFS 实现（Kubo vs IPFS Kubo Lite vs 自研） | – | T0 - 3 月 |
| 4 | HSM 厂商（YubiHSM / Thales / Ledger Enterprise） | – | T0 - 3 月 |
| 5 | 主链 ChainID 数值（避免与 ETH/BSC/Polygon 冲突） | 99 / 314 / 待选 | T0 - 1 月 |

---

— 本技术选型决策书完 —
